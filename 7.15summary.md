# 7.15 Summary

## 今日目标

今天主要围绕 Quest3 teleoperation 数据、StarVLA fine-tuning、Franka 真机 deployment 和 DAgger correction 展开。核心目标是让 StarVLA 在真实 Franka 上完成 “pick up the cube and place it on the box”，并重点修复目前暴露出来的两个问题：

- policy 容易向前/向下走，越过 cube；
- policy 对 cube 的横向偏移不够敏感，y/lateral correction 不稳定。

## 训练进展

今天完成了从更多 Quest3 Franka 数据继续 fine-tune StarVLA。

已经完成的模型：

- `quest3_franka_delta_eef_from_40eps2k_69eps_3k`
  - 基于原来的 40 episodes，加上后来筛过的新数据，总共约 69 episodes。
  - 训练 3000 steps 完成。
  - checkpoint:
    `results/Checkpoints/quest3_franka_delta_eef_from_40eps2k_69eps_3k/final_model/pytorch_model.pt`

- `quest3_franka_delta_eef_from_69eps3k_89eps_dagger20_2k`
  - 在 69eps 模型基础上，加了 20 条 DAgger-lite correction 数据。
  - 训练 2000 steps 完成。
  - W&B 显示最终大致：
    - `action_dit_loss`: 0.022
    - `mse_score`: 0.01267
  - checkpoint:
    `results/Checkpoints/quest3_franka_delta_eef_from_69eps3k_89eps_dagger20_2k/final_model/pytorch_model.pt`

之前遇到的 `future_tokens.weight` size mismatch 原因是 checkpoint 的 `action_chunk_size=32`，而当前 config 是 `8`。后面通过使用匹配的训练配置和 checkpoint 继续训练解决。

## Deployment 现象

今天多次做了 dry-run 和短步 execute。控制链本身基本是通的：

- policy server 可以在 server 上监听 `0.0.0.0:10093`；
- Franka client 可以拿到 image 和 `/current_pose`；
- dry-run 能输出 `action_chunk_shape: (8, 7)`；
- `/target_pose` execute 后 Franka 能动；
- direct gripper action 可以 open/close。

但真机行为还没有稳定成功。主要观察到：

- policy 经常输出 `x+` 和 `z-`，表现为从相机视角往前、往下走；
- `gripper_values` 经常保持 `[1. 1. ...]`，说明模型没有及时切到 close；
- 机器人有时会越过 cube，或者走到更像 box/place 的区域；
- y 轴不是完全不能动，实际 pose 里 y 可以变化很大，但模型没有稳定学会 “看到 cube 横向偏了就先横向对齐”。

对 `123.mp4` 的观察结论：

- 问题不是单纯 y 轴符号错；
- 更像是 policy 的任务阶段判断错了；
- 它有时像是进入了 place/box phase，而不是先找 cube、对齐、close、lift；
- 第三视角相机对 gripper-cube 的深度和相对位置判断仍然比较弱。

## DAgger 讨论和当前方案

今天明确了两个层次：

- 完整自动 DAgger 程序还没有写好。
- 当前已经能做的是 DAgger-lite。

DAgger-lite 流程：

1. 从 standard pose 或失败附近 pose 跑 StarVLA。
2. 发现方向明显错、越过 cube、或者 gripper 不 close 时，立即 `Ctrl-C` 停止 policy。
3. 从当前失败状态开始打开 recorder。
4. 用 Quest3 teleop 接管，把任务完成。
5. 保存成 LeRobot episode。
6. 把这些 correction episodes 加进训练集重新 fine-tune。

今天判断：之前 20 条 DAgger 数据有用，但比较像 general failure correction。现在需要更 targeted 的 lateral/y correction 数据。

理想的 targeted correction episode 应该是：

- gripper 已经接近 cube；
- cube 明显在 gripper 左边或右边；
- 第一段动作主要是横向移动到 cube 正上方；
- 对齐后再下降；
- 然后 close、lift、move to box、open。

## 新采集数据

今天开始收新的 targeted y/lateral correction 数据。

已经仔细检查并确认 keep 的新数据：

```text
0150 0152 0153 0154
```

这 4 条的检查结果：

- 视频完整，时长约 14-18 秒；
- 都有明显横向 correction；
- `action.gripper` 都有正常的 open -> close -> open；
- 视频里看起来都能抓到 cube 并移动到 box 附近/上方；
- 已经给本地数据补好了 `meta/modality.json`。

这些数据的 y range 很明显：

```text
0150: about 0.232 m
0152: about 0.348 m
0153: about 0.270 m
0154: about 0.334 m
```

这说明它们比普通完整任务更适合教模型 lateral correction。

后来你说已经收了 20 条，包括这 4 条。我开始检查 `0143-0162`，初步发现：

- `0144 / 0145 / 0155 / 0156` 本地缺完整数据；
- `0146 / 0147 / 0148 / 0149` 视频非常短，约 0.4-1.5 秒，基本不适合直接训练；
- `0150 / 0152 / 0153 / 0154` 已确认 keep；
- 剩余编号还需要继续筛选。

因此下一步不能盲目把 `0143-0162` 全部加进 training mixture，需要先筛出真正可用的 keep list。

## 数据格式和 modality

今天再次确认 StarVLA 需要 `meta/modality.json` 来把 LeRobot 数据里的 state/action 切成模型需要的字段。

关键 mapping 是：

```json
"gripper": {
  "start": 6,
  "end": 7,
  "original_key": "observation.state"
}
```

不能直接用 `observation.state.gripper`，因为它在当前 recorder 里是一维数组，之前会导致 dataloader 报：

```text
Expected 2D array, got key observation.state.gripper
```

目前一个老问题仍然存在：

- `observation.state.gripper` 经常全是 `0.0`；
- 但 `action.gripper` 是正确的，能看到 `1 -> 0 -> 1`；
- 所以这些数据仍然可以用来训练 gripper action。

## Wrist Camera / D435i 讨论

导师建议了两个方向：

1. 加 wrist D435i camera，重新训练双相机版本；
2. 做 DAgger，让模型失败时通过遥操接管并采集 correction data。

今天讨论了 MakerWorld：

- MakerWorld 是 Bambu Lab 相关的 3D 打印模型平台；
- 你们有 D435i camera，但缺的是固定到 Franka wrist 的 mount/bracket；
- MakerWorld 上看到的 D405 Franka mount 不适合直接用，因为你们没有 D405；
- “Intel RealSense 435i Mount - 20mm 80-20 Extrusion” 更像铝型材支架，不是 Franka wrist 直装；
- 短期最实际还是继续做 DAgger-lite；
- wrist camera 是中长期提升视觉定位能力的方向。

## 当前结论

今天最重要的结论是：

- StarVLA 控制链已经能跑通，不是完全通信或 ROS 问题；
- gripper 硬件能 open/close；
- y 轴控制本身也能动；
- 当前主要瓶颈是 policy 没有稳定学会视觉上的 cube alignment 和任务阶段切换；
- targeted DAgger 是当前最快修复路径；
- 新的 `0150 0152 0153 0154` 是好的 lateral correction 数据，值得加入训练；
- 新收的 20 条还需要先过滤，不能直接全部训练。

## 下一步

建议下一步按这个顺序做：

1. 继续检查新收的 20 条，筛出真正 keep 的 episode。
2. 给 keep episodes 全部补 `meta/modality.json`。
3. 把 keep episodes 同步到 server：
   `/data/hanyu/quest3_franka_real/snkdjn/`
4. 在 StarVLA registry 里加一个新的 mixture，比如：
   `quest3_franka_pick_cube_place_box_89eps_plus_lateral_dagger`
5. 从最新模型继续 fine-tune：
   `quest3_franka_delta_eef_from_69eps3k_89eps_dagger20_2k`
6. 训练 1000-2000 steps 先看效果。
7. 部署时先 dry-run，看 `dpos` 是否在 cube 偏左/偏右时出现更明显 y correction。
8. 再短步 execute，安全地验证是否能先横向对齐 cube，再下降 close。

如果下一轮还是经常越过 cube，继续收更窄的 correction：

- cube 在 gripper 左边；
- cube 在 gripper 右边；
- gripper 已经越过 cube；
- gripper 已经接近 cube 但没有 close；
- gripper close 后没有 lift。

这些比普通完整 episode 更能直接修复当前 failure。
