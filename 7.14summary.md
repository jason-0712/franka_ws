# 2026-07-14 Quest3 Franka StarVLA Summary

## 1. 今日目标

今天主要围绕一条完整链路推进：

1. 用 Meta Quest 3 遥操作 Franka 采集真实机器人 pick-and-place 数据。
2. 检查 LeRobot 数据是否适合 StarVLA fine-tuning。
3. 修复/补齐 StarVLA 对 Quest3 Franka 7D Cartesian delta EEF 数据的 registry、modality 和训练配置。
4. 用旧 40 条数据训练 2k steps 模型，并在 Franka 上 dry-run / execute 测试。
5. 继续采集新数据到 0112，筛选可用 episode，并准备训练 69eps 新模型。

整体结论：今天已经把 Quest3 Franka 数据格式、StarVLA 训练配置、部署 client 和数据筛选流程跑通。旧 40eps 模型已经能 approach cube，但 gripper close 和 move-to-box 还不稳定。新采集的 29 条可用数据质量明显更好，下一步应训练 69eps 模型。

## 2. 语言和交互约定

用户要求后续对话使用中文。

因此后续所有部署、训练、调试说明默认用中文写，并尽量提供完整可复制命令。

## 3. 数据格式确认

### 3.1 LeRobot 数据结构

用户提供/确认的 HuggingFace 数据集采用 LeRobot v2.1 格式：

- `meta/info.json`
- `meta/modality.json`，需要手动补齐或确认
- `data/chunk-000/episode_000000.parquet`
- `videos/chunk-000/observation.images.primary/episode_000000.mp4`

关键 features：

- `observation.images.primary`
  - video
  - 256 x 256 x 3
  - fps 15
  - codec av1
- `observation.state.cartesian`
  - shape `[6]`
  - names: `x, y, z, roll, pitch, yaw`
- `observation.state.gripper`
  - shape `[1]`
  - name: `gripper`
- `observation.state.joints`
  - shape `[7]`
- `observation.state.target`
  - shape `[6]`
- `observation.state`
  - shape `[20]`
  - concatenates cartesian, gripper, joints, target
- `action`
  - shape `[7]`
  - names: `x, y, z, roll, pitch, yaw, gripper`

### 3.2 是否涉及 RPY

数据中确实有 RPY 字段：

- state 里有 `roll, pitch, yaw`
- action 里也有 `roll, pitch, yaw`

但 Quest3 teleop 录制时我们建议使用：

```bash
-p rotation_scale:=0.0
```

也就是说：

- 数据 schema 里保留 RPY。
- 实际 teleop 不应该让 Quest 手柄姿态控制 Franka 姿态。
- action 的 RPY delta 应该接近 0，只保留当前控制框架所需的 7D 结构。

今天检查新数据时，候选 episodes 的 RPY action 都很小，说明这批新数据整体符合预期。

## 4. modality.json 的关键修复

StarVLA dataloader 读取 `observation.state.gripper` 时曾报错：

```text
AssertionError: Expected 2D array, got key observation.state.gripper is (...) array
```

原因是 StarVLA 的 modality slice 期望从一个 2D state array 中取 gripper，而不是直接读单独的 1D `observation.state.gripper`。

正确 mapping 是：

```json
"gripper": {
  "start": 6,
  "end": 7,
  "original_key": "observation.state"
}
```

不是：

```json
"original_key": "observation.state.gripper"
```

当前已添加脚本：

```bash
scripts/apply_quest3_franka_delta_eef_modality.py
```

这个脚本会给 Quest3 Franka delta EEF 数据写入正确 `meta/modality.json`。

今天已对新筛出来的 29 条可用数据补齐了 `modality.json`。其中 `0069` 之前已经补过，所以脚本显示 skipped 是正常的。

## 5. 本地新增/修改的重要文件

### 5.1 StarVLA Franka deployment client

文件：

```bash
scripts/starvla_franka_delta_pose_client.py
```

作用：

- 连接 StarVLA websocket policy server。
- 接收 image + current pose。
- 输出 7D Cartesian delta EEF action：

```text
[dx, dy, dz, droll, dpitch, dyaw, gripper]
```

主要特性：

- 默认 dry-run，不会发 `/target_pose`。
- 只有加 `--execute` 才会实际控制 Franka。
- 默认 image topic：

```bash
/right/right_third_person_camera/color/image_raw
```

- 默认 current pose topic：

```bash
/current_pose
```

- 默认 target pose topic：

```bash
/target_pose
```

- 默认 gripper command topic：

```bash
/gripper/gripper_position_controller/commands
```

- 有安全限制：
  - `--max-trans-delta`
  - `--max-rot-delta`
  - `--min-z`
  - `--max-z`
  - `--max-steps`
  - `--rate`

### 5.2 modality helper

文件：

```bash
scripts/apply_quest3_franka_delta_eef_modality.py
```

作用：

- 给符合 StarVLA 读取方式的 Quest3 Franka dataset 写 `meta/modality.json`。
- 重点修复 gripper mapping。

### 5.3 modality template

文件：

```bash
scripts/starvla_quest3_franka_delta_eef_modality.json
```

作用：

- 保存 Quest3 Franka delta EEF 的标准 modality schema。

### 5.4 recorder camera config

文件：

```bash
src/crisp_gym/crisp_gym/config/envs/third_person_cam_franka.yaml
```

作用：

- 让 recorder 使用第三视角 RealSense 作为 `observation.images.primary`。
- 避免录出无视频数据。

关键配置：

```yaml
control_frequency: 30.0
gripper_enabled: false
gripper_continuous_control: true
gripper_threshold: 0.1

robot_config:
  robot_type: "franka"
  time_to_home: 2.0

gripper_config:
  min_value: 0
  max_value: 1

camera_configs:
  - camera_name: "primary"
    camera_frame: "right_third_person_camera_color_optical_frame"
    resolution: [256, 256]
    camera_color_image_topic: "right/right_third_person_camera/color/image_raw"
    camera_color_info_topic: "right/right_third_person_camera/color/camera_info"
    max_image_delay: 1.0

sensor_configs: []
```

## 6. Quest3 teleop/recording 命令

### 6.1 Quest3 adapter 推荐命令

后续录制建议使用这个 adapter 命令：

```bash
cd /home/ros/ros2_ws
source /opt/ros/humble/setup.bash
source /home/ros/miniconda3/etc/profile.d/conda.sh
conda activate lerobot
export ROS_DOMAIN_ID=30
export PYTHONPATH=/home/ros/ros2_ws/src/crisp_gym:/home/ros/ros2_ws/src/crisp_py:$PYTHONPATH

python -m crisp_gym.scripts.quest3_stream_adapter --ros-args \
  -p quest_pose_topic:=/quest/right_controller/pose \
  -p quest_joy_topic:=/quest/right_controller/joy \
  -p translation_scale:=0.45 \
  -p x_scale:=1.1 \
  -p y_scale:=1.1 \
  -p z_scale:=1.4 \
  -p max_translation_step:=0.006 \
  -p rotation_scale:=0.0 \
  -p 'x_axis:="-x"' \
  -p 'y_axis:="z"' \
  -p 'z_axis:="y"' \
  -p deadman_button:=4 \
  -p deadman_latch_button:=-1 \
  -p reset_button:=0 \
  -p pause_button:=-1 \
  -p gripper_button:=6 \
  -p trigger_axis:=5 \
  -p trigger_threshold:=0.8 \
  -p gripper_release_delay:=0.08 \
  -p record_transition_topic:=/record_transition \
  -p record_button:=4 \
  -p save_button:=0 \
  -p delete_button:=1 \
  -p gripper_requires_deadman:=false \
  -p franka_gripper_command_topic:=/gripper/gripper_position_controller/commands
```

关键点：

- `rotation_scale:=0.0`：Quest 手柄姿态不控制 Franka 姿态。
- `reset_button:=0`：开始前可以 re-anchor。
- `gripper_release_delay:=0.08`：缩短 gripper release 延迟。
- `record_transition_topic:=/record_transition`：Quest 按键可以控制 recording manager。

### 6.2 recorder 推荐命令

录制单个 episode 时使用：

```bash
cd /home/ros/ros2_ws
source /opt/ros/humble/setup.bash
source /home/ros/miniconda3/etc/profile.d/conda.sh
conda activate lerobot
export ROS_DOMAIN_ID=30
export PYTHONPATH=/home/ros/ros2_ws/src/crisp_gym:/home/ros/ros2_ws/src/crisp_py:$PYTHONPATH

python -m crisp_gym.scripts.record_lerobot_format_leader_follower \
  --use-quest3-controller \
  --follower-config third_person_cam_franka \
  --streamed-pose-topic /phone_pose \
  --streamed-gripper-topic /phone_gripper \
  --recording-manager-type ros \
  --repo-id snkdjn/quest3_franka_tele_XXXX \
  --tasks "pick up the cube and place it on the box" \
  --num-episodes 1 \
  --fps 15 \
  --push-to-hub \
  --use-current-pose-as-episode-start \
  --streamed-teleop-timeout 60 \
  --disable-recorder-gripper-control
```

把 `XXXX` 换成新的 dataset id。

### 6.3 录完立即检查

必须看到视频：

```bash
ls /home/ros/.cache/huggingface/lerobot/snkdjn/quest3_franka_tele_XXXX/videos/chunk-000/observation.images.primary/
```

必须看到：

```bash
episode_000000.mp4
```

检查 info：

```bash
grep -n "total_videos\|observation.images.primary" \
  /home/ros/.cache/huggingface/lerobot/snkdjn/quest3_franka_tele_XXXX/meta/info.json
```

必须满足：

- `"total_videos": 1`
- 存在 `"observation.images.primary"`

如果 `total_videos: 0`，不要用于 StarVLA 视觉训练。

## 7. teleop 中“左移带逆时针旋转”的问题

### 7.1 今日现象

用户多次遇到：

- 移动 Quest controller 向左，Franka 有时会往相反方向或带 anticlockwise rotation。
- 有时重启 Franka controller 后恢复。
- 有一次重启后仍不正常，但等待一段时间后又正常。

### 7.2 当前判断

这个问题更像是初始化/同步时序问题，不是 StarVLA 模型问题，也不是 Quest3 一直在控制 RPY。

可能原因：

1. `/current_pose` 还没稳定。
2. recorder 内部 `target_pose` 还没从 current pose 初始化好。
3. Quest adapter 还没 re-anchor。
4. `/target_pose` 有旧 publisher 或之前的 target 残留。
5. 刚启动后太快按 deadman 运动，controller 还在追旧目标姿态。

因为用户观察到“什么都没做，等一下就好了”，这强烈说明是 target/current/re-anchor 的同步问题。

### 7.3 推荐录制前检查流程

停止旧进程：

```bash
pkill -f quest3_stream_adapter
pkill -f record_lerobot_format_leader_follower
pkill -f starvla_franka_delta_pose_client
```

检查 current pose：

```bash
ros2 topic echo /current_pose --once
```

检查 target pose publisher：

```bash
ros2 topic info -v /target_pose
```

建议：

- recorder/adapter 启动后等待 5-10 秒。
- 录制前按一次 reset/re-anchor button。
- 手柄静止 1 秒。
- 小幅测试左右、前后、上下。
- 确认没有旋转后再开始 episode。

### 7.4 诊断 RPY 是否被写入 action

移动左边时看：

```bash
ros2 topic echo /quest/right_controller/delta_end_pose
```

正常应该类似：

```text
data: [dx, dy, dz, 0.0, 0.0, 0.0, gripper]
```

第 4-6 个值是 RPY delta。它们应该接近 0。

如果明显不为 0：

- 检查 adapter 是否真的用了 `rotation_scale:=0.0`。
- 检查是否有旧 adapter 还在运行。

## 8. gripper 问题总结

### 8.1 训练/部署时 gripper output 常为 1

旧 40eps 模型部署时发现：

- `gripper_values` 经常全是 `[1, 1, ..., 1]`
- 机器人接近 cube 但不 close
- 或 close 时机不稳定

判断：

- 硬件 gripper 没坏，因为 direct action 可以 open/close。
- 更可能是旧 40eps 数据量不足或 close 动作分布不够稳定。
- 新 29 条数据中 gripper 都有明显 `1 -> 0 -> 1`，有助于改善。

### 8.2 direct gripper action

打开：

```bash
ros2 action send_goal /franka_gripper/move franka_msgs/action/Move \
  "{width: 0.08, speed: 0.1}"
```

抓取：

```bash
ros2 action send_goal /franka_gripper/grasp franka_msgs/action/Grasp \
  "{width: 0.0, epsilon: {inner: 0.005, outer: 0.04}, speed: 0.06, force: 80.0}"
```

如果夹爪推开 cube，可试：

```bash
ros2 action send_goal /franka_gripper/grasp franka_msgs/action/Grasp \
  "{width: 0.015, epsilon: {inner: 0.01, outer: 0.03}, speed: 0.03, force: 80.0}"
```

如果报：

```text
The passed action type is invalid
```

说明 terminal 没 source 到 Franka action type。先运行：

```bash
cd /home/ros/ros2_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
export ROS_DOMAIN_ID=30
ros2 interface show franka_msgs/action/Grasp
```

或进入 docker 后 source 再运行。

### 8.3 gripper delay 优化

Quest adapter 中：

```bash
-p gripper_release_delay:=0.08
```

Franka hand adapter open delay：

```bash
ros2 param set /crisp_py_franka_hand_adapter open_command_hold_sec 0.1
ros2 param get /crisp_py_franka_hand_adapter open_command_hold_sec
```

## 9. 旧 40eps 数据与模型

### 9.1 旧 40eps IDs

旧训练数据：

```text
0021
0024-0028
0030-0063
```

总计 40 条。

### 9.2 旧数据统计观察

以 `0063` 和旧 40 条统计为参考：

- camera 视角中 box 在左，cube 在右。
- approach cube 阶段 pre-close displacement mean 约：

```text
x +0.2836
y +0.1029
z -0.4600
```

- 所有旧 40 条 pre-close 的 y displacement 都为正。

因此在当前相机/机器人布局下：

```text
camera-right correction ~= base y positive
```

这个结论很重要。之前曾凭直觉怀疑 y 负是右，后来以数据统计为准修正为：camera-right 对应 base `y += ...`。

### 9.3 旧 40eps 训练结果

训练 run：

```text
quest3_franka_delta_eef_from_libero30k_40eps_2k
```

checkpoint：

```bash
results/Checkpoints/quest3_franka_delta_eef_from_libero30k_40eps_2k/final_model/pytorch_model.pt
```

W&B summary 里最后：

```text
action_dit_loss: 0.04401
mse_score: 0.03415
max_train_steps: 2000
```

训练速度大约：

```text
2000/2000 in 07:53
~4.22 it/s
```

### 9.4 旧 40eps 部署表现

优点：

- 能朝 cube 大概 approach。
- 有 forward/downward 趋势。
- 在手动对准时能接近 cube。

不足：

- gripper output 常为 1，不稳定 close。
- place-to-box 趋势弱。
- lateral correction 有时不足。
- 执行速度较慢，需要更多 steps。

结论：旧 40eps 模型可作为 warm start，但不够稳定。需要加入新 29 条更干净数据继续 fine-tune。

## 10. StarVLA server 与 client

### 10.1 server 命令

在 server：

```bash
cd /home/hanyu/starVLA
conda activate starVLA
export PYTHONPATH=$(pwd):$PYTHONPATH
export NO_ALBUMENTATIONS_UPDATE=1

CKPT=results/Checkpoints/quest3_franka_delta_eef_from_libero30k_40eps_2k/final_model/pytorch_model.pt
CUDA_VISIBLE_DEVICES=0 python deployment/model_server/server_policy.py \
  --ckpt_path "$CKPT" \
  --port 10093 \
  --use_bf16
```

正常应看到：

```text
server listening on 0.0.0.0:10093
```

### 10.2 deployment 需要的 terminals

通常需要 3-4 个 terminal：

1. Franka Cartesian controller terminal
2. StarVLA policy server terminal
3. StarVLA Franka client terminal
4. Optional status/debug terminal

### 10.3 Franka controller 命令

在 docker：

```bash
docker exec -it franka bash
cd /home/ros/ros2_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
export ROS_DOMAIN_ID=30

ros2 launch /home/ros/ros2_ws/src/crisp_controllers_demos/crisp_controllers_robot_demos/launch/franka_cartesian_impedance.launch.py \
  arm_id:=fr3 \
  robot_ip:=172.16.0.2 \
  use_fake_hardware:=false \
  use_rviz:=false
```

### 10.4 dry-run 观察重点

client dry-run 输出中重点看：

- `action_chunk_shape: (8, 7)`
- `translation_min`
- `translation_max`
- `rpy_min`
- `rpy_max`
- `gripper_values`
- 每个 step 的 `dpos`, `drpy`, `target_pos`, `gripper`

如果 gripper 一直是：

```text
gripper_values: [1. 1. 1. 1. 1. 1. 1. 1.]
```

说明模型当前没有预测 close。

如果 `dy` 没有明显正确趋势，说明模型没有进入 lateral correction 或视觉判断不够稳定。

## 11. 新数据采集与筛选

### 11.1 用户今天新采集到 0112

用户说新采集了 30 条左右，到 `0112`，中间有些数字没用。

本地目录中看到：

```text
0065
0066
0067
0069
0070-0100
0102-0112
```

其中很多是 0 帧或无视频，需要筛选。

### 11.2 不可用新数据

不要用于 StarVLA 视觉训练：

```text
0065
0066
0067
0070
0073
0074
0080
0083
0088
0091
0093
0096
0098
0099
0102
0103
0104
```

原因：

- 0 frames
- no parquet
- no video
- `total_videos: 0`
- 没有 `observation.images.primary`

尤其：

- `0066` 有 parquet 但没有 video，不适合当前 StarVLA 视觉训练。
- `0067` 同样没有 video。

### 11.3 可用新数据

可用于 69eps 训练的新数据：

```text
0069
0071
0072
0075
0076
0077
0078
0079
0081
0082
0084
0085
0086
0087
0089
0090
0092
0094
0095
0097
0100
0105
0106
0107
0108
0109
0110
0111
0112
```

总计 29 条。

### 11.4 新数据统计

这些 29 条都满足：

- 有 parquet
- 有 video
- 有 `observation.images.primary`
- action shape `[7]`
- gripper 有 `0.0` 和 `1.0`
- 有 clear close/open transition
- close 后有明显 lift
- RPY action 小

部分统计：

```text
0069: frames=301, close=100, open_after=252, zlift=+0.262
0071: frames=243, close=99,  open_after=196, zlift=+0.206
0072: frames=201, close=87,  open_after=162, zlift=+0.179
0075: frames=227, close=95,  open_after=183, zlift=+0.184
0076: frames=244, close=129, open_after=205, zlift=+0.175
0077: frames=226, close=107, open_after=186, zlift=+0.228
0078: frames=203, close=90,  open_after=158, zlift=+0.171
0079: frames=216, close=112, open_after=176, zlift=+0.179
0081: frames=209, close=93,  open_after=164, zlift=+0.212
0082: frames=215, close=97,  open_after=171, zlift=+0.202
0084: frames=228, close=87,  open_after=182, zlift=+0.229
0085: frames=257, close=131, open_after=205, zlift=+0.237
0086: frames=218, close=114, open_after=178, zlift=+0.192
0087: frames=221, close=100, open_after=177, zlift=+0.169
0089: frames=261, close=133, open_after=217, zlift=+0.233
0090: frames=223, close=104, open_after=178, zlift=+0.212
0092: frames=227, close=103, open_after=184, zlift=+0.205
0094: frames=216, close=116, open_after=174, zlift=+0.233
0095: frames=227, close=115, open_after=188, zlift=+0.185
0097: frames=234, close=125, open_after=192, zlift=+0.163
0100: frames=205, close=97,  open_after=162, zlift=+0.182
0105: frames=210, close=107, open_after=173, zlift=+0.185
0106: frames=218, close=105, open_after=179, zlift=+0.193
0107: frames=208, close=109, open_after=168, zlift=+0.186
0108: frames=199, close=99,  open_after=160, zlift=+0.196
0109: frames=216, close=100, open_after=166, zlift=+0.262
0110: frames=205, close=86,  open_after=165, zlift=+0.204
0111: frames=226, close=108, open_after=181, zlift=+0.218
0112: frames=224, close=113, open_after=180, zlift=+0.228
```

结论：新 29 条比旧数据更稳定，更适合改善 gripper close 和 place-to-box。

## 12. 新数据打包传 server

用户最开始尝试：

```bash
for id in $GOOD_NEW_IDS; do
  scp -r ...
done
```

问题：

- 需要输入 29 次密码。
- 中途密码输入错误导致 shell 断行和路径被弄乱。
- 出现：

```text
Permission denied
No such file or directory
hanyu@192.168.1.113s: command not found
```

推荐方式：tar 打包一次，然后 scp 一次。

### 12.1 在 Franka 电脑打包

```bash
cd /home/dase-hw101/franka_ws/dataset/snkdjn

tar -czf /tmp/quest3_franka_good_new_0069_0112.tar.gz \
  quest3_franka_tele_0069 \
  quest3_franka_tele_0071 \
  quest3_franka_tele_0072 \
  quest3_franka_tele_0075 \
  quest3_franka_tele_0076 \
  quest3_franka_tele_0077 \
  quest3_franka_tele_0078 \
  quest3_franka_tele_0079 \
  quest3_franka_tele_0081 \
  quest3_franka_tele_0082 \
  quest3_franka_tele_0084 \
  quest3_franka_tele_0085 \
  quest3_franka_tele_0086 \
  quest3_franka_tele_0087 \
  quest3_franka_tele_0089 \
  quest3_franka_tele_0090 \
  quest3_franka_tele_0092 \
  quest3_franka_tele_0094 \
  quest3_franka_tele_0095 \
  quest3_franka_tele_0097 \
  quest3_franka_tele_0100 \
  quest3_franka_tele_0105 \
  quest3_franka_tele_0106 \
  quest3_franka_tele_0107 \
  quest3_franka_tele_0108 \
  quest3_franka_tele_0109 \
  quest3_franka_tele_0110 \
  quest3_franka_tele_0111 \
  quest3_franka_tele_0112
```

### 12.2 scp 一次

```bash
scp /tmp/quest3_franka_good_new_0069_0112.tar.gz \
  hanyu@192.168.1.113:/data/hanyu/quest3_franka_real/snkdjn/
```

### 12.3 server 解压

```bash
ssh hanyu@192.168.1.113
cd /data/hanyu/quest3_franka_real/snkdjn
tar -xzf quest3_franka_good_new_0069_0112.tar.gz
```

### 12.4 server 检查

```bash
ls quest3_franka_tele_0112/meta/info.json
ls quest3_franka_tele_0112/meta/modality.json
ls quest3_franka_tele_0112/data/chunk-000/episode_000000.parquet
ls quest3_franka_tele_0112/videos/chunk-000/observation.images.primary/episode_000000.mp4
```

## 13. StarVLA registry/config 状态

### 13.1 robot_type

已配置：

```text
quest3_franka_delta_eef
```

对应：

```text
Quest3FrankaDeltaEefDataConfig
```

### 13.2 action/state keys

StarVLA dataloader 成功显示：

```text
Used action keys (reordered): ['delta_eef_position', 'delta_eef_rotation', 'gripper']
Used state keys (reordered): ['eef_position', 'eef_rotation', 'gripper']
```

说明 registry 和 modality 已经能正确识别。

### 13.3 旧 mixture

旧 mixture：

```text
quest3_franka_pick_cube_place_box_40eps
```

包含旧 40 条：

```text
0021
0024-0028
0030-0063
```

### 13.4 新 mixture

需要在 server 上加入：

```text
quest3_franka_pick_cube_place_box_69eps
```

包含：

- 旧 40 条
- 新 29 条可用数据

总计 69 条。

## 14. server 上添加 69eps registry 的命令

在 server：

```bash
cd /home/hanyu/starVLA
conda activate starVLA
export PYTHONPATH=$(pwd):$PYTHONPATH

python - <<'PY'
from pathlib import Path
import re

path = Path("examples/Franka/train_files/data_registry/data_config.py")
if not path.exists():
    path = Path("examples/realRobots/Franka/train_files/data_registry/data_config.py")

old_ids = [21, *range(24, 29), *range(30, 64)]
new_ids = [69, 71, 72, 75, 76, 77, 78, 79, 81, 82, 84, 85, 86, 87, 89, 90, 92, 94, 95, 97, 100, 105, 106, 107, 108, 109, 110, 111, 112]
all_ids = old_ids + new_ids

entries = "".join(
    f'        ("quest3_franka_tele_{i:04d}", 1.0, "quest3_franka_delta_eef"),\n'
    for i in all_ids
)

block = f'    "quest3_franka_pick_cube_place_box_69eps": [\n{entries}    ],\n'
text = path.read_text()

pattern = r'    "quest3_franka_pick_cube_place_box_69eps": \[\n.*?    \],\n'
if re.search(pattern, text, flags=re.S):
    text = re.sub(pattern, block, text, flags=re.S)
else:
    text = text.replace(
        '    "quest3_franka_pick_cube_place_box_debug": [\n',
        block + '    "quest3_franka_pick_cube_place_box_debug": [\n',
    )

path.write_text(text)
print("updated", path)
print("episodes", len(all_ids))
PY
```

## 15. dataloader sanity check

server 上运行：

```bash
cd /home/hanyu/starVLA
conda activate starVLA
export PYTHONPATH=$(pwd):$PYTHONPATH

python starVLA/dataloader/lerobot_datasets.py \
  --config_yaml examples/Franka/train_files/starvla_cotrain_quest3_franka_delta_eef.yaml \
  --data_root_dir /data/hanyu/quest3_franka_real/snkdjn \
  --data_mix quest3_franka_pick_cube_place_box_69eps
```

用户最新反馈：

```text
Used state keys (reordered): ['eef_position', 'eef_rotation', 'gripper']
Processing Batches: 0%| 4/10385 [00:00<25:32, 6.77it/s
```

这说明：

- dataset path 正确。
- registry 正确。
- `modality.json` 正确。
- state/action key 正确。
- gripper 不再触发 1D array error。
- video 能被读取。

这是好消息。等待 dataloader 完成后会生成：

```bash
results/debug/dataset_statistics.json
```

## 16. 69eps 训练命令

推荐从旧 40eps 2k checkpoint 继续训练。

server 上运行：

```bash
cd /home/hanyu/starVLA
conda activate starVLA
export PYTHONPATH=$(pwd):$PYTHONPATH
export NO_ALBUMENTATIONS_UPDATE=1

DATA_ROOT=/data/hanyu/quest3_franka_real/snkdjn \
DATA_MIX=quest3_franka_pick_cube_place_box_69eps \
RUN_ID=quest3_franka_delta_eef_from_40eps2k_69eps_3k \
MAX_TRAIN_STEPS=3000 \
SAVE_INTERVAL=500 \
GPU_IDS=0 \
PRETRAINED_CHECKPOINT=results/Checkpoints/quest3_franka_delta_eef_from_libero30k_40eps_2k/final_model/pytorch_model.pt \
bash examples/Franka/train_files/run_quest3_franka_train_delta_eef.sh
```

如果想快速 smoke test，可先：

```bash
MAX_TRAIN_STEPS=1000
```

正式建议：

```bash
MAX_TRAIN_STEPS=3000
```

## 17. 训练参数解释

### 17.1 max_train_steps

`max_train_steps` 指 optimizer update 的总步数，不是 episode 数。

例如：

```text
MAX_TRAIN_STEPS=3000
```

表示模型参数更新 3000 次。

由于当前 batch size 小，69 条数据会被重复采样多轮。它不是只看每条数据一次。

### 17.2 save_interval

```text
SAVE_INTERVAL=500
```

表示每 500 steps 保存一次 checkpoint。

最终模型在：

```bash
results/Checkpoints/<RUN_ID>/final_model/pytorch_model.pt
```

## 18. 今日遇到的主要错误与解决

### 18.1 numpy missing

错误：

```text
ModuleNotFoundError: No module named 'numpy'
```

原因：

- 在 base 环境或错误 Python 环境运行 Quest reader/bridge。

解决：

- 进入 docker。
- activate `lerobot`。
- 设置 ROS env 和 PYTHONPATH。

### 18.2 OculusReader missing

错误：

```text
ImportError: Could not import OculusReader
```

原因：

- 没有进入正确环境。
- 或没有正确 `PYTHONPATH`。

解决：

```bash
source /home/ros/miniconda3/etc/profile.d/conda.sh
conda activate lerobot
export PYTHONPATH=/home/ros/ros2_ws/src/piper-vr-teleop:$PYTHONPATH
```

### 18.3 dataset path not found

错误：

```text
FileNotFoundError: Dataset path /data/hanyu/quest3_franka_real/snkdjn/quest3_franka_tele_0063 does not exist
```

原因：

- server 上还没同步数据。

解决：

- 从 Franka 电脑把 dataset 传到：

```bash
/data/hanyu/quest3_franka_real/snkdjn
```

### 18.4 gripper 1D array error

错误：

```text
AssertionError: Expected 2D array, got key observation.state.gripper
```

解决：

- modality 中 state gripper 从 `observation.state` 第 6 维 slice。

### 18.5 checkpoint path wrong

错误：

```text
No such file or directory: /path/to/your/libero_or_ibero_30000_rerun/pytorch_model.pt
```

找到真实 checkpoint：

```bash
/data/hanyu/starVLA_checkpoints/libero_all_gr00t_official_30000_rerun/final_model/pytorch_model.pt
```

### 18.6 action horizon mismatch

错误：

```text
size mismatch for action_model.future_tokens.weight:
checkpoint shape [32, 768], current model shape [8, 768]
```

原因：

- 当前 config 的 action horizon/target tokens 与 checkpoint 不兼容。

解决：

- 使用与 checkpoint 兼容的 `num_target_vision_tokens: 32`。
- 后续训练脚本已围绕这个配置修过。

### 18.7 policy server connection refused

错误：

```text
ConnectionRefusedError: [Errno 111] Connection refused
```

原因：

- client 启动时 server 没有在 port 10093 监听。

解决：

- 先启动 server，看到：

```text
server listening on 0.0.0.0:10093
```

再启动 client。

## 19. Meta Quest 3 充电问题

今天用户问 Quest3 是否能用 power bank 充电，以及手机 app 是否显示 charging。

结论：

- 可以用 power bank 给 Meta Quest 3 充电。
- 手机 app 不一定及时显示 charging 状态。
- 头显橙色灯亮通常表示正在充电。
- 使用官方 USB 和 charger 时看到橙色灯，说明充电正常。

## 20. 数据采集质量建议

后续继续采数据时建议：

1. 保持相机视角和物体布局一致。
2. 当前训练布局：camera 视角中 box 在左，cube 在右。
3. 不要混入相反布局，除非专门做双方向泛化。
4. 每条都必须成功，不成功不要用于训练。
5. close 前让 gripper 明确到 cube 两侧。
6. close 后停 0.5-1 秒，让数据中 closed 状态明显。
7. lift 要明显，不要刚夹住就横移。
8. place 阶段要清楚地朝 box 运动。
9. open/drop 动作要明显。
10. episode start pose 尽量一致。
11. 出现异常旋转的 episode 不要用于 fine-tuning。
12. 录完马上检查 video 和 gripper transition。

## 21. 推荐数据量

当前：

- 旧可用：40 eps
- 新可用：29 eps
- 总计：69 eps

建议：

- 69 eps 可以训练下一版并部署测试。
- 更稳目标：100 eps。
- 如果 place-to-box 仍弱，建议额外采 30 条 place-focused episode：
  - 初始已抓住 cube
  - 只训练 lift + move to box + open/drop

更长期：

- 150-200 eps 会更稳。
- 但真实机器人任务里质量比数量更重要。

## 22. 明天/下一步计划

### Step 1: 等 dataloader sanity check 跑完

确认无报错，并生成：

```bash
results/debug/dataset_statistics.json
```

### Step 2: 训练 69eps 3k steps

运行：

```bash
DATA_ROOT=/data/hanyu/quest3_franka_real/snkdjn \
DATA_MIX=quest3_franka_pick_cube_place_box_69eps \
RUN_ID=quest3_franka_delta_eef_from_40eps2k_69eps_3k \
MAX_TRAIN_STEPS=3000 \
SAVE_INTERVAL=500 \
GPU_IDS=0 \
PRETRAINED_CHECKPOINT=results/Checkpoints/quest3_franka_delta_eef_from_libero30k_40eps_2k/final_model/pytorch_model.pt \
bash examples/Franka/train_files/run_quest3_franka_train_delta_eef.sh
```

### Step 3: 找 final checkpoint

```bash
find results/Checkpoints/quest3_franka_delta_eef_from_40eps2k_69eps_3k \
  -type f -name "pytorch_model.pt" | sort
```

应该看到：

```bash
results/Checkpoints/quest3_franka_delta_eef_from_40eps2k_69eps_3k/final_model/pytorch_model.pt
```

### Step 4: 启动 policy server

```bash
cd /home/hanyu/starVLA
conda activate starVLA
export PYTHONPATH=$(pwd):$PYTHONPATH
export NO_ALBUMENTATIONS_UPDATE=1

CKPT=results/Checkpoints/quest3_franka_delta_eef_from_40eps2k_69eps_3k/final_model/pytorch_model.pt
CUDA_VISIBLE_DEVICES=0 python deployment/model_server/server_policy.py \
  --ckpt_path "$CKPT" \
  --port 10093 \
  --use_bf16
```

### Step 5: dry-run 部署

先不要 execute。

观察：

- 是否 approach cube。
- 是否在接近 cube 后 gripper 从 1 变 0。
- close 后是否有 lift。
- lift 后是否有 move-to-box 趋势。
- RPY delta 是否小。

### Step 6: 小步 execute

如果 dry-run 正常，再 execute：

- 低速度
- 小 delta
- 少 steps
- 手放 E-stop

建议先：

```text
max_steps: 20-40
rate: 2 Hz
max_trans_delta: 0.003-0.004
max_rot_delta: 0.001
```

不要一上来跑 100 steps。

## 23. 当前最重要结论

1. 旧 40eps pipeline 已跑通。
2. StarVLA 7D Cartesian delta EEF client 已可用。
3. 数据格式关键问题是 `modality.json`，尤其 gripper mapping。
4. 新采集数据中只有 29 条适合视觉训练。
5. 新 29 条质量不错，gripper 和 lift 都明显。
6. 现在 dataloader 已开始 Processing Batches，说明 69eps mixture 基本成功。
7. 下一步应该训练 `quest3_franka_delta_eef_from_40eps2k_69eps_3k`。
8. 训练后先 dry-run，再小步 execute。
9. 如果 place-to-box 仍弱，下一批重点采 place-focused episodes。

