# 2026-07-08 Quest 3 -> Franka Teleoperation Detailed Summary

## 1. 今日总体目标

7 月 8 日的主要目标是把 Quest 3 右手柄接入 Franka FR3 teleoperation，并让它可以用于后续 LeRobot / StarVLA 风格的数据采集。

目标链路是：

```text
Quest 3 right controller
-> Quest native OpenXR passthrough/tracking app
-> adb logcat
-> quest_reader_ros_bridge
-> /quest/right_controller/pose
-> /quest/right_controller/joy
-> quest3_stream_adapter
-> /phone_pose
-> /phone_gripper
-> record_lerobot_format_leader_follower
-> /target_pose
-> cartesian_impedance_controller
-> Franka FR3 movement
```

最终希望采集的数据 action space 是 7D delta end pose：

```text
[dx, dy, dz, droll, dpitch, dyaw, gripper]
```

这个方向比 8D delta joint 更适合 Quest 3，也更接近 StarVLA / LIBERO 原始 action space。

## 2. 今天完成了什么

今天主要完成了以下几件事：

1. Quest 3 ADB 连接恢复正常。
2. Quest native tracking app 能启动。
3. Quest passthrough 从黑屏问题恢复到可见真实环境。
4. `adb logcat` 能看到 Quest right controller tracking 数据。
5. `quest_reader_ros_bridge.py` 能发布：

```text
/quest/right_controller/pose
/quest/right_controller/joy
```

6. `/quest/right_controller/pose` 能稳定 30 Hz。
7. `quest3_stream_adapter.py` 增加了 axis mapping / scale / button recording control。
8. recorder 增加了 `--skip-home`，用于坐标轴调试时临时避免自动回初始位。
9. 新增/使用 Franka Cartesian impedance launch，让 `/target_pose` 能走 Cartesian controller。
10. Franka 出现 force/torque threshold、Joint Position Error 后，完成了一轮 error recovery。

## 3. Recording Control 的演进：Keyboard -> Quest Controller

今天不是一开始就直接做 Quest controller 控制 recording。实际过程是先用 keyboard recording 把主链路验证通，然后再考虑把 recording 操作迁移到 Quest controller 上。

### 第一阶段：Keyboard recording

最开始使用的是 keyboard recording manager。

命令形式类似：

```bash
python -m crisp_gym.scripts.record_lerobot_format_leader_follower \
  --use-quest3-controller \
  --follower-config no_cam_franka \
  --streamed-pose-topic /phone_pose \
  --streamed-gripper-topic /phone_gripper \
  --recording-manager-type keyboard \
  --repo-id quest3_franka_test \
  --num-episodes 1 \
  --fps 15 \
  --no-push-to-hub
```

keyboard manager 的交互是：

```text
r = start / stop recording
s = save current episode
d = delete current episode
q = quit
```

这个阶段的意义是：

```text
先验证 Quest pose -> /phone_pose -> recorder -> Franka target 的主链路。
```

当时 keyboard recording 已经能进入：

```text
Waiting to start recording...
Keyboard command received: r
Resetting Environment.
Started recording episode.
```

所以可以认为：

```text
keyboard recording path 基本可用。
```

### 为什么后来想改成 controller recording

keyboard recording 虽然能工作，但实际戴着 Quest headset 操作时很不方便：

- 操作者戴着 headset，看不到 keyboard。
- 一只手拿 controller，另一只手去按 keyboard 会破坏 teleop 姿态。
- 如果要做自然、连续的数据采集，最好所有操作都在 controller 上完成。
- 未来真实采集 episode 时，操作者应该能在 headset 内完成 start / stop / save / delete。

所以我们开始改成：

```text
Quest controller buttons
-> /quest/right_controller/joy
-> quest3_stream_adapter
-> /record_transition
-> recorder
```

### 第二阶段：ROS recording manager

controller recording 使用：

```text
--recording-manager-type ros
```

并由 adapter 发布：

```text
/record_transition
```

今天检查到：

```text
/record_transition
Type: std_msgs/msg/String
Publisher count: 1
Subscription count: 1
```

这说明：

```text
adapter 和 recorder 的 recording-control topic 已经接上。
```

### 原计划的 controller 操作方式

原本想实现：

```text
右摇杆按下一次 = teleop start
再按一次 = teleop stop
中指 right grip 按下 = start recording
松开 right grip = stop recording
A = save
B = delete
```

这个设计更接近自然 teleop：

```text
右摇杆控制是否遥操作
right grip 控制是否记录
A/B 控制 episode 是否保留
```

### 为什么没有继续用右摇杆按下

实际测试发现 right joystick click 没有稳定进入 `/quest/right_controller/joy`。

按下右摇杆后，检查：

```bash
ros2 topic echo /quest/right_controller/joy --once
```

看到的 `buttons[5]` 仍然经常是：

```text
0
```

因此如果继续依赖 right joystick click，就会出现：

```text
以为 teleop 已经打开，但 adapter 实际没有收到 button。
```

这会让调试变得很混乱，所以临时放弃 right joystick click。

### 当前采用的 controller 操作方式

当前更可靠的版本是：

```text
按住 right grip = teleop enabled + recording active
松开 right grip = teleop stop + recorder pause
A = save
B = delete
```

这个版本的优点：

- 不依赖不稳定的 right joystick click。
- right grip 比较容易从 Joy topic 里识别。
- 松开手指 robot 就停止，更安全。
- 戴 headset 时不需要碰 keyboard。

缺点：

- 必须一直按住 right grip，手会更累。
- teleop 和 recording 现在绑定在一起，不是完全独立。
- 松开 right grip 后必须按 A 或 B 决定 save/delete，不能直接开始下一段。

### Keyboard vs Controller 对比

| 项目 | Keyboard recording | Controller recording |
| --- | --- | --- |
| 启动方式 | 终端按 `r` | 按住 right grip |
| 停止方式 | 再按 `r` | 松开 right grip |
| 保存 | `s` | A button |
| 删除 | `d` | B button |
| 优点 | 简单、稳定、容易 debug | 戴 headset 时自然，不用看键盘 |
| 缺点 | 戴 Quest 时操作不方便 | 依赖 button mapping，调试复杂 |
| 当前状态 | 已测试可用 | topic 已接通，但 body movement 仍需继续排查 |

### 当前结论

今天的正确理解是：

```text
keyboard recording 是 baseline，证明 recorder 本身可用。
controller recording 是改进版，用来提升采集时的人机交互。
```

如果 controller recording 出问题，应该先回到 keyboard recording 做 sanity check，确认 robot/controller 主链路没有坏。

## 4. Quest Headset Passthrough 黑屏问题

### 现象

戴上 Quest 3 后，一开始经常看到：

```text
全黑
透明但没有 UI
突然黑屏
只有 controller battery warning
```

当时误以为可能是左 controller 低电量导致，但后来确认：

```text
黑屏不是左 controller 电量低导致的。
```

左 controller 低电量最多会弹 warning，不应该导致 Quest app tracking 完全失败。

### 正确认知

Quest app 有两个层面：

```text
tracking data layer
passthrough rendering/UI layer
```

所以：

```text
headset 黑屏 != Quest tracking 一定没工作
```

判断 app 是否真的工作，应该看 logcat，而不是只看 headset 画面。

### 检查 Quest app 是否在运行

```bash
adb devices
adb shell pidof com.rail.oculus.teleop
```

正常应该看到设备：

```text
2G97C5ZHB603FS    device
```

如果 `pidof` 输出一个数字，例如：

```text
26092
```

说明 Quest app 进程正在运行。

### 检查 Quest 是否 awake

```bash
adb shell dumpsys power | grep -Ei 'mWakefulness|Display Power'
```

如果看到：

```text
mWakefulness=Asleep
```

说明 headset 睡眠了。可以唤醒：

```bash
adb shell input keyevent KEYCODE_WAKEUP
```

然后再检查：

```bash
adb shell dumpsys power | grep -Ei 'mWakefulness|Display Power'
```

期望：

```text
mWakefulness=Awake
```

注意：摘下 headset 后它很快会再次 sleep，所以调试 tracking 时最好戴着 headset。

### 检查 tracking log

```bash
adb logcat -v time -s wE9ryARX:I '*:S'
```

如果看到类似：

```text
I/wE9ryARX: right: ... |hmd: ...
```

说明：

```text
Quest app 正在输出 right controller 和 hmd tracking data。
```

### 检查 passthrough 权限/状态

```bash
adb logcat -d -t 300 | grep -Ei "Passthrough|XR_FB|xrCreatePassthrough|xrPassthrough|wE9ryARX"
```

今天后来看到系统 capability 里：

```text
Passthrough: ON
```

这说明 passthrough 权限/能力是打开的。

### 最后解决方式

实际恢复流程是：

1. 确认 ADB 是 `device`，不是 `unauthorized`。
2. 戴上 headset，保持 headset awake。
3. 重新启动 Quest app：

```bash
cd /home/ros/ros2_ws/src/piper-vr-teleop
scripts/launch_quest_mr_passthrough_app.sh
```

4. 用 logcat 确认 tracking 有输出：

```bash
adb logcat -v time -s wE9ryARX:I '*:S'
```

5. 后来 headset 内 passthrough 恢复，可以看到 Franka 和真实环境。

结论：

```text
Passthrough 黑屏时，先不要改 ROS。
先确认 ADB、Quest app process、wakefulness、logcat tracking、Passthrough: ON。
```

## 5. Quest ROS Bridge

启动 Quest bridge：

```bash
cd /home/ros/ros2_ws/src/piper-vr-teleop
source /opt/ros/humble/setup.bash
source /home/ros/miniconda3/etc/profile.d/conda.sh
conda activate lerobot
export ROS_DOMAIN_ID=30
export PYTHONPATH=/home/ros/ros2_ws/src/piper-vr-teleop:$PYTHONPATH

python3 scripts/quest_reader_ros_bridge.py \
  --transport adb_logcat \
  --rate 30
```

正常输出应包含：

```text
Publishing PoseStamped on /quest/right_controller/pose
Publishing Joy on /quest/right_controller/joy
```

检查 pose 频率：

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=30

ros2 topic hz /quest/right_controller/pose
```

今天已经确认：

```text
average rate: 30.000
```

这说明 Quest right controller pose 已经稳定进入 ROS。

检查 buttons：

```bash
ros2 topic echo /quest/right_controller/joy --once
```

当前 bridge 的 button mapping 是：

```text
buttons[0] = A
buttons[1] = B
buttons[2] = X
buttons[3] = Y
buttons[4] = rightGrip
buttons[5] = right joystick click
buttons[6] = rightTrig
```

今天发现：

```text
right joystick click 不稳定，按了以后 buttons[5] 仍可能是 0。
```

所以当前暂时不用 right joystick click 做 teleop toggle。

## 6. Adapter 参数：scale / xyz / rotation 怎么调

adapter 负责把 Quest controller movement 转成 `/phone_pose`。

当前推荐启动命令：

```bash
cd /home/ros/ros2_ws
source /opt/ros/humble/setup.bash
source /home/ros/miniconda3/etc/profile.d/conda.sh
conda activate lerobot
export ROS_DOMAIN_ID=30
export PYTHONPATH=/home/ros/ros2_ws/src/crisp_gym:/home/ros/ros2_ws/src/crisp_py:$PYTHONPATH

python -m crisp_gym.scripts.quest3_stream_adapter --ros-args \
  -p quest_pose_topic:=/quest/right_controller/pose \
  -p quest_joy_topic:=/quest/right_controller/joy \
  -p translation_scale:=0.4 \
  -p max_translation_step:=0.008 \
  -p rotation_scale:=0.0 \
  -p 'x_axis:=-x' \
  -p 'y_axis:=z' \
  -p 'z_axis:="y"' \
  -p deadman_button:=4 \
  -p deadman_latch_button:=-1 \
  -p reset_button:=-1 \
  -p pause_button:=-1 \
  -p record_transition_topic:=/record_transition \
  -p record_button:=4 \
  -p save_button:=0 \
  -p delete_button:=1 \
  -p gripper_requires_deadman:=false \
  -p franka_gripper_command_topic:=/gripper/gripper_position_controller/commands
```

### translation_scale

```text
translation_scale 控制 controller 位移放大多少。
```

今天用过：

```text
translation_scale = 0.4
```

如果 Franka 动得太小：

```text
0.4 -> 0.6 -> 0.8
```

如果 Franka 太敏感：

```text
0.4 -> 0.3 -> 0.2
```

建议每次只改一点，不要一次从 0.4 改到 2.0。今天已经触发过 force/torque threshold，所以 real robot 上不要一开始用太大 scale。

### max_translation_step

```text
max_translation_step 限制每一帧最多能移动多少。
```

当前：

```text
max_translation_step = 0.008
```

这相当于每次最多 8 mm 左右。它用于防止 Quest controller 突然跳变导致 Franka 突然大幅运动。

如果 movement 太慢，可以小幅调大：

```text
0.008 -> 0.010 -> 0.012
```

如果容易触发力/力矩安全错误，可以调小：

```text
0.008 -> 0.005
```

### rotation_scale

当前：

```text
rotation_scale = 0.0
```

含义：

```text
暂时不让 Quest controller orientation 控制 Franka wrist orientation。
```

今天主要调 position，不调 RPY。原因是 position 方向还在校准，如果同时打开 rotation，会更难判断问题来自哪里。

之后如果要打开 wrist rotation，可以从小值开始：

```text
rotation_scale = 0.2
```

不要一开始设成 1.0。

### x_axis / y_axis / z_axis

这三个参数决定 Quest controller 的运动轴如何映射到 Franka base frame。

当前比较接近可用的一组是：

```text
x_axis = -x
y_axis = z
z_axis = y
```

命令里写法：

```bash
-p 'x_axis:=-x' \
-p 'y_axis:=z' \
-p 'z_axis:="y"'
```

理解方式：

```text
x_axis 控制 Franka x 方向来自 Quest 哪个轴
y_axis 控制 Franka y 方向来自 Quest 哪个轴
z_axis 控制 Franka z 方向来自 Quest 哪个轴
```

前面加 `-` 表示反向。

### 如何判断该改哪个轴

只改一个轴，不要同时改多个。

如果：

```text
controller 向左 -> Franka 向右
controller 向右 -> Franka 向左
```

说明 left/right 对应的轴反了。把对应轴加负号或去掉负号。

如果：

```text
controller 向前 -> Franka 向后
controller 向后 -> Franka 向前
```

说明 front/back 对应的轴反了。把对应轴加负号或去掉负号。

如果：

```text
controller 向上 -> Franka 向下
controller 向下 -> Franka 向上
```

说明 up/down 对应的轴反了。把对应轴加负号或去掉负号。

今天最终观察：

```text
up/down 已经正确
left/right 和 front/back 曾经反过
```

因此后续如果继续调，优先检查 `x_axis` 和 `z_axis`，不要先动 `y_axis`。

## 7. 当前按钮逻辑

一开始想做：

```text
右摇杆按下一次 = teleop start
再按一次 = teleop stop
中指 grip 按下 = record start
松开 grip = record stop
A = save
B = delete
```

但 right joystick click 在 `/quest/right_controller/joy` 中没有稳定变成 1，所以改成当前更安全的模式：

```text
按住 right grip = teleop enabled + recording
松开 right grip = teleop stop + recorder pause
A = save
B = delete
```

注意：

```text
Franka body 要动，必须一直按住 right grip。
```

如果松开 right grip，gripper 可能还能响应，但 body teleop 会停止。

检查 right grip 是否被识别：

```bash
ros2 topic echo /quest/right_controller/joy --once
```

按住 right grip 时应该看到：

```text
buttons[4] = 1
```

如果仍然是 0，adapter 不会放行 body movement。

## 8. Recorder

当前 recorder 命令：

```bash
cd /home/ros/ros2_ws
source /opt/ros/humble/setup.bash
source /home/ros/miniconda3/etc/profile.d/conda.sh
conda activate lerobot
export ROS_DOMAIN_ID=30
export PYTHONPATH=/home/ros/ros2_ws/src/crisp_gym:/home/ros/ros2_ws/src/crisp_py:$PYTHONPATH

python -m crisp_gym.scripts.record_lerobot_format_leader_follower \
  --use-quest3-controller \
  --follower-config no_cam_franka \
  --streamed-pose-topic /phone_pose \
  --streamed-gripper-topic /phone_gripper \
  --recording-manager-type ros \
  --repo-id quest3_997 \
  --num-episodes 1 \
  --fps 15 \
  --no-push-to-hub \
  --skip-home
```

当时调试时加了：

```text
--skip-home
```

原因：

```text
开始 recording 自动回初始位会让 robot motion 和 coordinate transform 变得难判断。
```

所以当前不让 recorder 在开始录制时自动 home。

当前修正：Quest 3 采集不再让 recorder 自动回标准位。标准位先通过
leader-arm workflow 完成，然后 Quest 3 recorder 使用 `--skip-home`，把当前
Franka pose 当作本轮 episode 的起点。这样可以避免 Quest 3 recorder 卡在
`/controller_manager/list_controllers` 或 homing controller switch 上。

## 9. Franka Controller 问题

### 现象：只有 gripper 动，Franka body 不动

今天遇到过：

```text
gripper 可以动
Franka body 不动
```

原因是使用了不适合 Quest body teleop 的 launch：

```bash
ros2 launch crisp_controllers_robot_demos franka.launch.py ...
```

这个 launch 会让 body movement path 不对，导致 `/target_pose` 没有被正确的 active Cartesian controller 执行。

### 正确 launch

Quest 3 body teleop 应该启动 Cartesian impedance controller：

```bash
docker exec -it franka bash
cd /home/ros/ros2_ws
source /opt/ros/humble/setup.bash
source /home/ros/ros2_ws/install/setup.bash
export ROS_DOMAIN_ID=30

ros2 launch /home/ros/ros2_ws/src/crisp_controllers_demos/crisp_controllers_robot_demos/launch/franka_cartesian_impedance.launch.py \
  arm_id:=fr3 \
  robot_ip:=172.16.0.2 \
  use_fake_hardware:=false \
  use_rviz:=false
```

### 不要过度依赖 ros2 control list_controllers

今天 `/controller_manager/list_controllers` 经常 timeout，所以不要只用：

```bash
ros2 control list_controllers
```

判断是否成功。

更可靠的检查：

```bash
ros2 topic info -v /target_pose
ros2 topic echo /current_pose --once
```

期望：

```text
/target_pose 有 publisher
/target_pose 有 cartesian_impedance_controller subscriber
/current_pose 能输出当前 Franka end-effector pose
```

## 10. Franka 报错和恢复

### 今天出现过的错误

今天 Franka 出现过：

```text
configured force threshold reached
configured torque threshold reached
Joint Position Error detected
X3.1 Triggered
X4 Not Enabled
```

这些通常来自：

```text
scale 太大
坐标方向反了
robot 被推到不舒服的方向
Desk safety/recovery 状态没有完全恢复
external enabling device 没按住
```

### 第一原则

一旦 Desk 出红色 robot error：

```text
先停止所有 teleop。
不要继续动 Quest controller。
不要继续 recording。
```

建议停掉：

```text
quest3_stream_adapter
record_lerobot_format_leader_follower
Franka controller launch
```

### ROS error recovery command

今天成功执行过：

```bash
docker exec -it franka bash -lc 'source /opt/ros/humble/setup.bash && source /home/ros/ros2_ws/install/setup.bash && export ROS_DOMAIN_ID=30 && ros2 action send_goal /action_server/error_recovery franka_msgs/action/ErrorRecovery "{}"'
```

成功输出：

```text
Goal accepted
Goal finished with status: SUCCEEDED
```

但这个只能解决一部分 robot error。如果 Desk 进入 Joint Position Error recovery，还需要在 Desk UI 里继续处理。

### Joint Position Error recovery

Desk 显示过：

```text
Joint Position Error detected
Joints are misaligned
Please resolve the joint position error by moving to the reference position
```

流程大意：

1. 确认周围安全。
2. 如果 X3.1 triggered，先释放 emergency device。
3. 按照 Desk 指示处理对应 joint。
4. X4 是 external enabling device，需要按住物理 enabling device。
5. 对有 error 的 joint，按照 Desk 提示 Start Recovery / Move Joint / Confirm Recovery。
6. 如果不确定，必须找 tutor / safety operator。

注意：

```text
不要自己随便用手强行掰 robot joint。
```

只有 Desk 明确要求、且安全 operator 确认时，才按 recovery 流程操作。

### Shut Down 卡住

今天 Desk 点击 Shut Down 后出现长时间 connecting / Finished shutting down。

处理思路：

```text
不要继续 teleop
确认 Desk 状态
必要时按 tutor 指导用 private browsing 重新登录 Desk
```

Tutor 提到 private browsing 的意思是：

```text
用浏览器的无痕窗口重新打开 Franka Desk，避免旧 session/cookie 导致 logout 或 login 不响应。
```

## 11. 当前最后卡住点

最后看到：

```bash
ros2 topic info /record_transition
ros2 topic info /target_pose
```

结果：

```text
/record_transition:
Publisher count: 1
Subscription count: 1

/target_pose:
Publisher count: 1
Subscription count: 1
```

这说明 topic graph 已经连上。

但是 Franka body 仍然不动，所以接下来要判断：

```text
1. right grip 是否真的 buttons[4] = 1
2. /target_pose 是否随 controller movement 改变
3. /current_pose 是否随 /target_pose 改变
4. Desk 是否仍然处于 recovery/safety 状态
```

### 下一步排查命令

按住 right grip：

```bash
ros2 topic echo /quest/right_controller/joy --once
```

确认：

```text
buttons[4] = 1
```

然后按住 right grip 并移动 controller：

```bash
ros2 topic echo /target_pose --field pose.position
```

如果 `/target_pose` 不变：

```text
问题在 Quest / adapter / recorder 侧。
```

如果 `/target_pose` 变了，再看：

```bash
ros2 topic echo /current_pose --field pose.position
```

如果 `/target_pose` 变但 `/current_pose` 不变：

```text
问题在 Franka controller / Desk safety / robot execution 侧。
```

如果两个都变，但肉眼觉得 robot 动得很小：

```text
scale 太小，可以把 translation_scale 从 0.4 小幅调到 0.6。
```

## 12. 明天继续建议

明天不要一上来录数据，先按下面顺序做 30 秒 dry test：

1. Desk 确认没有红色 error。
2. 启动 Franka Cartesian controller。
3. 启动 Quest app。
4. 启动 Quest ROS bridge。
5. 启动 adapter。
6. 不启动 recorder，先检查 `/phone_pose` 是否跟 controller movement 变化。
7. 启动 recorder。
8. 按住 right grip，检查 `/target_pose`。
9. 再检查 `/current_pose`。
10. 确认 motion direction 和 scale 都正常后，再正式录 episode。

最重要原则：

```text
一次只改一个参数。
先调方向，再调 scale。
先小幅 movement，再录正式数据。
Franka 一报红色 error，立即停 teleop。
```
