# Franka–Quest 3–StarVLA 工作总结（2026-07-23、2026-07-24、2026-07-27）

## 一、项目目标与系统范围

本阶段的目标是在真实 Franka FR3 上完成如下视觉语言操作任务：

> pick up the cube and place it on the box

系统由以下部分组成：

- Franka FR3 机械臂与 Franka 两指夹爪；
- Quest 3 控制器，用于遥操作示范采集；
- 第三人称 Intel RealSense D455F；
- 腕部 Intel RealSense D455F；
- ROS 2 Humble、CRISP 控制环境与 LeRobot 数据格式；
- StarVLA 策略服务器和真实机器人 delta-pose 客户端；
- 主机工作区：`/home/dase-hw101/franka_ws`；
- 机器人容器：`franka`；
- 双相机容器：`franka_dual_realsense`。

三个工作日的主线是：

1. 7 月 23 日：打通双相机 Quest 3 遥操作采集，并修复夹爪观测数据；
2. 7 月 24 日：完成 50 条双相机示范的筛选、合并、训练方案重构，并开始真实部署；
3. 7 月 27 日：完善真实 Franka 部署客户端，分析抓取、抬升与释放行为，并区分安全过滤成功与纯 VLA 成功。

---

# 2026-07-23：双相机遥操作、夹爪反馈修复与大规模候选数据采集

## 1. 当日目标

- 确认 Quest 3 能稳定连接并向 ROS 2 发布控制数据；
- 确认第三人称与腕部相机同时工作；
- 解决按下控制器后不能开始录制、Franka 不运动及夹爪不能关闭的问题；
- 修复 `observation.state.gripper` 始终为 0 的数据问题；
- 开始采集可用于双相机 StarVLA 微调的真实示范。

## 2. Quest 3 连接与控制输入

Quest 3 通过 USB 被 ADB 正确识别：

- 设备序列号：`2G97C5ZHB603FS`；
- 型号：Quest 3；
- Quest 无线地址：`192.168.1.149`。

调试过程中确认了几个容易混淆的问题：

- `QUEST_IP` 只是占位符，不能把尖括号或变量名原样作为命令执行；
- `adb devices -l` 显示设备并不代表应用层 bridge 已经发布 ROS 数据；
- `/quest/right_controller/joy` 出现 `Publisher count: 0` 时，说明 Quest bridge 没有真正发布，而不是 adapter 的订阅逻辑错误；
- Quest trigger 在 Joy 消息中表现为 axis，而不是 button；
- 按压过程中最后一个 axis 会在 `1`、`0`、`-1` 等值之间变化，不能仅检查 `buttons` 数组；
- `/phone_gripper` 最终采用了适合现有 LeRobot 动作约定的开关语义：`1 = open`、`0 = close`。

当日多次遇到 Quest bridge 表面“卡住”的情况。实际需要区分两种状态：

- 正常等待数据：终端没有持续打印，但 ROS topic 有 publisher；
- ADB/bridge 未建立：终端没有数据且 ROS topic publisher 为 0。

## 3. 双 RealSense 相机调试

双相机配置目标如下：

- 第三人称相机序列号：`344522302659`；
- 腕部相机序列号：`351322301561`；
- 第三人称压缩图像 topic：
  `/right/right_third_person_camera/color/image_raw/compressed`；
- 腕部压缩图像 topic：
  `/right/right_wrist_camera/color/image_raw/compressed`。

主要故障包括：

- `Timeout waiting for camera (wrist) to become ready`；
- `Camera wrist image data is stale`；
- RealSense 日志中的设备断连和 `xioctl: No such device`；
- Docker/DDS 层看似存在 publisher，但 recorder 收不到新帧；
- 双相机处理使 recorder 实际速度下降到约 10–11 FPS，而目标设置为 15 FPS。

处理措施包括：

- 使用 `docker-compose.dual-franka.yaml` 固定两台相机序列号；
- 重建 `franka_dual_realsense` 容器；
- 检查 host 是否同时看到两台 `8086:0b5c` RealSense；
- 检查两个压缩图像 topic 是否各有一个 publisher；
- 延长相机初始化等待时间；
- 避免旧的 `realsense_ros2` 容器与新双相机容器冲突；
- 识别到腕部相机 USB 线缆、接口、供电或机械应力是间歇断连的重要风险。

当相机正常时，双相机容器观察到约 17–20 Hz 的有效图像到达率。Recorder 的 10–11 FPS 警告主要来自图像解码、缩放和两路数据写入的总处理时间，不等同于相机完全断流。

## 4. 录制启动与自动保存/上传问题

当日排查了以下录制问题：

- 按下控制器不能开始 recording；
- 录制进程已经存在或残留，导致新 recorder 行为异常；
- bridge、adapter、controller、recorder 四个终端中的任一环节未运行；
- `Skipping automatic homing before starting with recording` 被误认为程序卡住；
- 录制完成后希望自动保存并自动 push 到 Hugging Face。

最终明确了正常录制所需的运行链：

1. Franka Cartesian controller；
2. Quest reader bridge；
3. Quest 3 stream adapter；
4. LeRobot recorder；
5. 双相机服务由 Docker 后台运行，不需要第五个交互式终端。

同时明确录制与部署不能同时运行，尤其不能有两个 `/target_pose` publisher。

## 5. `observation.state.gripper` 始终为 0 的根因与修复

这是 7 月 23 日最重要的数据质量问题。

原先为了避免 recorder 与外部 adapter 同时控制夹爪，使用了“禁用 recorder 夹爪控制”的逻辑。但旧实现把“停止发送夹爪命令”和“停止读取夹爪反馈”混在一起，导致：

- 真实夹爪可能已经开合；
- action 中也可能包含开/关命令；
- 但 `observation.state.gripper` 仍始终为 0。

完成的代码修复：

- `ManipulatorCartesianEnv.step()` 新增 `control_gripper` 参数；
- `control_gripper=False` 时只跳过夹爪命令发送；
- 环境仍从真实夹爪反馈生成 observation；
- `make_teleop_streamer_fn()` 将外部控制标志正确传入环境；
- action 使用 Quest stream 的最新夹爪值；
- 真实状态采用闭合程度：完全打开接近 0，逐渐闭合接近 1。

增加了回归测试：

- 禁止 recorder 发夹爪命令时，夹爪 feedback 仍保留；
- `gripper.value=0.25` 时 observation 正确转换为约 `0.75` 的闭合程度；
- stream adapter 的外部控制标志被正确转发。

相关文件：

- `src/crisp_gym/crisp_gym/envs/manipulator_env.py`；
- `src/crisp_gym/crisp_gym/record/record_functions.py`；
- `src/crisp_gym/tests/test_streamed_gripper_feedback.py`。

## 6. Episode 检查与数据质量筛选

当日逐步检查了多个候选 episode，包括：

- `0014`；
- `0019`；
- `0034`；
- `0036`；
- `0047` 前后的候选数据。

检查内容包括：

- 主相机视频是否完整；
- 腕部相机视频是否完整；
- 末端轨迹是否完成“接近—抓取—抬升—搬运—释放”；
- action.gripper 是否存在正确开关；
- `observation.state.gripper` 是否随物理夹爪变化；
- 图像、状态和 action 是否时间对齐；
- 是否存在相机 stale、录像中断或异常恢复动作。

`0036` 被明确判为不可用于最终微调：

- `observation.state.gripper` 全程为 0；
- 后续合并时明确排除该 episode。

## 7. 当日数据产出

7 月 23 日创建了 69 个双相机候选单 episode 数据目录，编号大致覆盖：

- `0002` 至 `0073`；
- 中间存在跳号；
- 这些是候选目录，不代表 69 条全部合格。

最终 50 条合并数据中，有 12 条来自 7 月 23 日：

`0047, 0048, 0049, 0050, 0051, 0052, 0058, 0060, 0063, 0064, 0065, 0069`

## 8. 7 月 23 日结论

- 双相机 Quest 3 遥操作链路基本打通；
- 相机 USB 稳定性仍是主要硬件风险；
- Trigger 必须按 axis 处理；
- recorder 是否控制夹爪与 recorder 是否读取夹爪状态必须解耦；
- 从 `0047` 以后开始获得可进入最终数据集的高质量候选；
- Episode 数量不能代替质量检查，尤其要检查真实夹爪 observation。

---

# 2026-07-24：完成 50-episode 数据集、重构训练方案并启动真实部署

## 1. 当日目标

- 完成剩余双相机示范采集；
- 从独立单 episode 目录中选择 50 条可靠示范；
- 合并为一个标准 LeRobot 数据集；
- 决定正确的 StarVLA 初始化 checkpoint；
- 完成 dry-run 和首次真实 Franka 部署；
- 建立安全停止与返回标准位姿流程。

## 2. 上午录制故障与恢复

上午首先遇到 `dual_cam_franka.yaml` YAML 解析错误：

- 报错位置约在第 34 行；
- recorder 无法创建环境；
- 修复 YAML 格式后恢复双相机录制。

同时继续处理：

- Quest bridge/ADB 可见但不可访问；
- recorder 等待 observation；
- wrist camera stale；
- gripper 偶发 uninitialised；
- controller 和 recorder 的启动顺序。

当天继续检查了 `0077` 及其后续候选 episode，并根据双相机完整性、夹爪反馈和任务轨迹筛选最终数据。

## 3. 当日新增数据

7 月 24 日创建了 41 个双相机候选目录，编号大致覆盖：

- `0076` 至 `0116`。

最终 50 条数据中，有 38 条来自 7 月 24 日，从 `0077` 延续到 `0116`。

## 4. 最终合并数据集

最终数据集：

`/home/dase-hw101/franka_ws/dataset/snkdjn/quest3_franka_dualcam_pickplace_50eps`

基本信息：

- LeRobot codebase version：v2.1；
- Robot type：Franka；
- Episodes：50；
- Frames：10,743；
- Tasks：1；
- Videos：100；
- 每条 episode 两路视频；
- 数据 FPS：15；
- Task：`pick up the cube and place it on the box`；
- 明确排除：`0036`。

最终 source ID 清单：

`0047, 0048, 0049, 0050, 0051, 0052, 0058, 0060, 0063, 0064, 0065, 0069, 0077, 0078, 0079, 0080, 0081, 0082, 0083, 0084, 0085, 0086, 0087, 0088, 0089, 0090, 0091, 0092, 0094, 0095, 0096, 0097, 0098, 0099, 0100, 0101, 0102, 0103, 0104, 0105, 0106, 0107, 0109, 0110, 0111, 0112, 0113, 0114, 0115, 0116`

数据特征包含：

- `observation.images.primary`；
- `observation.images.wrist`；
- `observation.state.cartesian`；
- `observation.state.gripper`；
- `observation.state.joints`；
- `observation.state.target`；
- 7D action：XYZ、RPY、gripper。

## 5. 训练方案选择

最初准备从已有真实 Franka checkpoint 继续微调：

- 旧模型主要来自单相机或缺少当前腕部相机分布的数据；
- dry-run 中夹爪预测在同一 action chunk 内频繁出现 `1/0` 交替；
- 继承旧真实模型可能同时继承原先的前冲、横向修正弱和夹爪时序问题。

因此最终选择 Scheme A：

- 从原始 LIBERO 30k checkpoint 开始；
- 不加载此前单相机 Franka checkpoint；
- 使用当前 50 条真实双相机数据直接适配；
- 使用新的 AdamW optimizer。

训练输入 checkpoint：

`/data/hanyu/starVLA_checkpoints/libero_all_gr00t_official_30000_rerun/final_model/pytorch_model.pt`

该 checkpoint 在 LIBERO 仿真评估中报告约 98.3% 成功率，但该结果不能直接代表真实 Franka 成功率。

训练输出：

`/data/hanyu/starVLA_runs/quest3_franka_dualcam_50eps_from_libero30k_10k`

关键训练配置：

- 10,000 steps；
- 每 2,000 steps 保存；
- GPU 1；
- batch size 1；
- repeated diffusion steps 8；
- action model learning rate `1e-5`；
- warmup 500 steps；
- `FREEZE_MODULES=qwen_vl_interface`；
- previous single-camera Franka checkpoint：不使用。

## 6. Modality 与输入输出定义

双相机 modality 明确为：

- state.eef_position：Cartesian state 0:3；
- state.eef_rotation：Cartesian RPY 3:6；
- state.gripper：combined state 第 7 维；
- action.delta_eef_position：delta XYZ；
- action.delta_eef_rotation：delta RPY；
- action.gripper：绝对开关；
- primary image：第三人称相机；
- wrist image：腕部相机。

夹爪语义：

- policy action `1 = open`；
- policy action `0 = close`；
- state gripper `0 = open`；
- state gripper 越大表示越闭合。

## 7. Policy server 与 dry-run

部署过程中首先遇到：

`OSError: [Errno 98] address already in use ('0.0.0.0', 10093)`

处理方式：

- 识别 10093 已被旧服务占用；
- 新 policy server 改用 `192.168.1.113:10094`；
- 客户端同步改为 port 10094。

Dry-run 验证了：

- 两路相机都进入 observation；
- policy metadata 返回 Cartesian delta action；
- action chunk shape 为 `(8, 7)`；
- server 能返回 state/action normalization metadata；
- 不加 `--execute` 时不会发布 `/target_pose`。

## 8. 首次真实 Franka 部署

真实部署早期采用非常保守的运动参数：

- 低 rate；
- 短 max steps；
- 小 translation clamp；
- gripper disabled；
- 逐步确认 `/current_pose` 确实发生变化。

随后逐步提高到 2、3、4 Hz，并在接近方块时减小单步位移。

观察结果：

- Franka 确实在移动，但初期变化很小，肉眼难以察觉；
- 快速 translation 会造成 Cartesian tracking lag；
- pitch 会偏离训练分布，因此接近阶段暂时冻结 rotation；
- 末端可以接近方块，但容易从方块上方或一侧越过；
- 模型的关闭信号具有连续性，但关闭位置仍然过早。

在一次启用夹爪的尝试中：

- 关闭确认位置约为 `[0.4671, -0.0088, 0.1401]` m；
- 模型连续预测 close；
- 夹爪最终完全闭合到约 `0.00008 m`/finger；
- 相机和宽度反馈证明这是空抓，方块仍在夹爪下方。

## 9. 真实部署客户端的重要修改

7 月 24 日建立或修改了以下能力：

- 发送真实 proprioceptive state；
- server 端使用训练统计量归一化 Franka state；
- 图像裁剪和预处理与训练保持一致；
- gripper chunk consensus；
- 多次请求 debounce；
- `--initial-gripper-state`；
- `--disable-gripper`；
- `--translation-scale`；
- delta translation/rotation 硬限制；
- 重复发布 held target pose；
- 相机 QoS 诊断和 frame timestamp 检查。

相关文件包括：

- `scripts/starvla_franka_delta_pose_client.py`；
- `scripts/franka_move_to_pose.py`；
- `scripts/capture_ros_camera_frames.py`；
- `src/camera_driver/docker-compose.dual-franka.yaml`；
- StarVLA server state-normalization 相关文件。

## 10. 停止与安全返回

实验结束时：

- StarVLA client 已退出；
- `/target_pose` publisher count 为 0；
- Cartesian impedance controller 保持 active；
- 机械臂通过先垂直抬升、再平滑返回的方式接近标准起始位姿。

最终测得位置约：

`[0.31115, 0.00355, 0.57013]` m

参考标准训练起点约：

`[0.30885, 0.00086, 0.58484]` m

当日遗留的关键问题是夹爪空抓后完全闭合，并且通过旧 hand adapter 重发 open 命令仍不能可靠张开。下一次部署前必须恢复真实夹爪控制与 feedback。

## 11. 7 月 24 日结论

- 50 条双相机 LeRobot 数据集已正式建立；
- 正确训练路径是从 LIBERO 30k 初始化，而不是继续沿用缺腕相机的旧真实模型；
- 仿真 98.3% 成功率不能直接迁移到真实系统；
- 首次部署证明双相机模型能够产生合理方向的 Cartesian action，但视觉对齐与夹爪时序尚不足；
- 必须使用真实指宽判断空抓，不能只相信 policy close logit；
- 安全返回脚本和 controller/publisher 检查是后续实验的必要组成部分。

---

# 2026-07-27：真实部署闭环、抓取状态机、严格 VLA 评估与数据分布诊断

## 1. 当日目标

- 使用完成训练的 50-episode dual-camera checkpoint 部署到真实 Franka；
- 提高运动速度和平滑性；
- 接入真实 Franka 指宽反馈；
- 解决过早闭合、空抓、无法抬升和反复抓取；
- 判断成功 episode 是否属于纯 VLA；
- 判断模型是否真正学会根据图像定位方块。

使用的最终 checkpoint：

`/data/hanyu/starVLA_runs/quest3_franka_dualcam_50eps_from_libero30k_10k/final_model/pytorch_model.pt`

Policy server：

`192.168.1.113:10094`

## 2. 控制器、标准位姿与进程管理

真实部署前反复确认：

- Cartesian impedance controller 必须保持 active；
- `/target_pose` 只能有一个 publisher；
- client 退出后 publisher count 应回到 0；
- camera、controller 和 gripper feedback 必须同时新鲜；
- 每次新 episode 前应回到一致的标准初始位姿。

新增/完善：

- `scripts/franka_return_to_standard.py`；
- 分段、限速、平滑返回标准位姿；
- 避免直接从桌面附近斜线返回；
- 明确 controller 可以继续运行，但 StarVLA client 与 return script 不能同时发布目标。

## 3. 从微小运动到更平滑的真实执行

最初部署中，末端位置确实变化，但肉眼变化很小，例如：

- 约 `[0.3084, 0.0019, 0.5834]`；
- 下降到 `[0.3079, 0.0015, 0.5791]`；
- 随后约 `[0.3078, 0.0047, 0.5665]`。

为提高可见速度，逐步调整：

- rate；
- max steps；
- execution horizon；
- translation scale；
- max translation delta；
- target publish rate。

一次典型长部署使用约 5 Hz、400 steps，并在约 step 270 由用户按 Ctrl-C 停止。视频文件：

`/home/dase-hw101/Downloads/279cd9d5c570ab680070f0ca38ab09cd.mp4`

该次运行表明：

- 机械臂可以接近方块；
- 夹爪位置已接近目标；
- 但 gripper disabled 或夹爪状态逻辑不正确时不会闭合。

随后将较稳定的执行方式调整到：

- action rate 约 10 Hz；
- target publish rate 约 40 Hz；
- execution horizon 8；
- translation scale 1.0；
- max translation delta 约 6 mm；
- max rotation delta 约 3 mrad；
- 推理时间计入 action cycle，避免每个 chunk 额外停顿。

修正 action 定时后，机械臂运动明显更连续、更平滑。

## 4. 接入真实 Franka 夹爪反馈

客户端改为订阅：

`/franka_gripper/joint_states`

真实总指间宽度计算：

`total_width = finger_joint1 + finger_joint2`

发送给 policy 的闭合状态：

`state_gripper_closed = clip(1 - total_width / 0.08, 0, 1)`

因此：

- 完全打开约 80 mm，对应 state 接近 0；
- 完全闭合接近 0 mm，对应 state 接近 1；
- 夹住当前方块时通常约 28–30 mm，对应 state 约 0.63–0.65。

同时解决或绕过了：

- `Timed out waiting for observations: ['gripper_state']`；
- policy 同步推理期间 ROS callback 未及时处理造成的假 stale；
- gripper node 缺失 `joint_names` 参数导致启动失败；
- client 使用命令状态代替真实物理状态的问题。

## 5. 抓取验证状态机的逐步演化

为了避免真实硬件反复空抓和碰撞，客户端逐步加入：

- 抓取后真实宽度验证；
- 空抓检测；
- 夹爪稳定时间；
- 抬升高度验证；
- 单 episode 最大抓取次数；
- 抓取闭合阶段的有限 XY/Z 运动范围；
- stale observation 检查；
- workspace、单步 translation、rotation 限制。

当前主要阈值：

- 完全空抓阈值：总宽度 `< 15 mm`；
- 有效方块抓取宽度：`22–36 mm`；
- 宽度验证开始延迟：1.2 s；
- 进入有效宽度范围的 timeout：3.0 s；
- 稳定宽度要求：0.25 s；
- 抓取确认所需真实抬升：30 mm；
- 抬升 timeout：5.0 s；
- 最大抓取次数：1；
- 未验证闭合阶段最大 XY drift：12 mm；
- 最大下降：14 mm；
- 最大上升：18 mm。

曾根据成功示范加入固定 XYZ close gate，后来扩大到：

- X：`0.480–0.516` m；
- Y：`-0.022–0.048` m；
- Z：`0.115–0.155` m。

但该 gate 使用末端绝对位置，而不是检测方块位置，会阻止模型对移动后的方块进行泛化。7 月 27 日最终已将这套 object-specific XYZ close gate 完全移除：

- 旧 CLI 参数已删除；
- `gripper_close_blocked` 路径已删除；
- 模型 close 决策不再依赖固定成功示范坐标。

曾讨论由客户端在宽度验证后自动执行固定 `+30 mm` 抬升，但最终未保留这一逻辑，因为它会由脚本替模型完成关键任务步骤，不能用于纯 VLA 能力结论。当前 30 mm 只用于验证模型实际产生的抬升。

## 6. 一次完成抓取、抬升和释放的 episode

移除固定 XYZ close gate 后，一次部署出现以下行为：

- Physical grasp attempt：1 次；
- 关闭时测得位置约：`[0.46443, -0.00215, 0.14247]` m；
- close target origin 约：`[0.47859, -0.01262, 0.13000]` m；
- 真实夹爪宽度稳定在约 `28.25 mm`；
- 宽度保持在有效范围 `22–36 mm`；
- 实际抬升约 `32.92 mm`，超过 30 mm 要求；
- 模型随后给出 open，并在箱子区域释放；
- 现场/视频若确认方块最终在箱子上，则任务层面完成。

这次运行中：

- `gripper_close_blocked = 0`；
- `grasp_closing_motion_limited = 0`；
- 没有固定 XYZ close gate；
- 没有脚本自动抬升；
- 末端轨迹、抓取、搬运与后续释放主要来自 VLA。

但是仍发生：

- `gripper_open_held_closed = 2`。

也就是说模型在完成宽度和抬升验证前两次要求打开，旧安全逻辑替模型保持了闭合。完成释放后 client 继续运行，模型再次请求抓取，最终被 `max attempts=1` 中止。

因此该 episode 的正确标签是：

`Safety-filtered VLA success`

而不是：

`Pure VLA success`

## 7. 严格 VLA 模式修改

为了进行更严格的模型能力评估，完成了以下最终修改：

- 删除 `gripper_open_held_closed`；
- 模型在宽度验证或 30 mm 抬升完成前要求打开时，记录：
  `EPISODE FAILURE`；
- 为避免在高处故意掉落，失败的 open 命令不发送到真实夹爪；
- 有效抓取、抬升并第一次请求 release 后，发布 open；
- 记录 `EPISODE COMPLETE`；
- 完成首次 validated release 后立即退出，不再等待第二次抓取；
- `EPISODE COMPLETE` 只代表完成抓取—抬升—释放链，方块是否确实到达箱子仍需视频或现场确认。

`max attempts=1` 仍保留为真实机器人安全中止条件，但在首次有效 release 后 client 已结束，所以不会再因任务完成后的第二次 close 而误报。

## 8. 最新严格评估失败 episode

严格模式运行中：

- 模型在 step 202 首次输出 close；
- 该 chunk 后 6 个动作保持 close；
- 总关闭保持时间只有约 0.52 s；
- 下一次 action chunk 为：`[1, 1, 0, 1, 1, 1, 0, 0]`；
- chunk 的第一步重新请求 open；
- 当时实时指宽反馈仍约 79.86 mm，尚未形成有效抓取；
- client 正确记录：
  `EPISODE FAILURE: policy requested opening before ... 0.030m lift`；
- client 随即退出，所以没有继续抬升。

退出后物理夹爪继续完成之前收到的 close 命令，随后测得：

- 总指宽约 28.26 mm；
- 末端位置约 `[0.45541, 0.03992, 0.14294]` m；
- `/target_pose` publisher 为 0；
- 很可能已夹住方块，但 client 已按严格规则判失败，因此没有抬升。

这个结果证明：

- 以前的 `gripper_open_held_closed` 确实掩盖了模型的提前打开问题；
- 当前模型的 gripper 时序与真实夹爪闭合延迟不匹配；
- 在纯 VLA 评估标准下，该 episode 应记为失败；
- 若重新加入 close latch，可以提高真实完成率，但必须标注为安全过滤而不是纯模型能力。

## 9. “是否作弊”与 VLA 归因结论

不同逻辑需要分别判断：

| 逻辑 | 性质 | 是否替模型完成任务 |
|---|---|---|
| 全局 workspace/min-z | 真实机器人安全边界 | 否 |
| 单步 delta clipping | 部署适配与安全限制 | 否，但必须报告 |
| 指宽/抬升验证 | 监测、成功或失败判定 | 通常否 |
| `max attempts=1` | 安全中止 | 否 |
| 固定成功示范 XYZ close gate | Object-specific 规则 | 会限制泛化，已删除 |
| 自动固定 +30 mm 抬升 | 脚本化任务步骤 | 未保留 |
| `gripper_open_held_closed` | 覆盖模型 open 输出 | 是主动干预，已从严格模式删除 |

因此：

- 之前完成放置的 episode 仍然是 VLA 驱动；
- 它不是预设 Cartesian 轨迹，也没有自动抬升；
- 但它有两次夹爪动作覆盖，所以不能计入纯 VLA success rate；
- 应单独记录为 safety-filtered success。

## 10. 模型是否真正学会定位方块

用户观察到，无论方块如何移动，夹爪似乎经常前往相似区域。为验证该问题，对最终 50 条训练数据做了分布分析。

训练数据第一次 close-command 的末端 XYZ：

- mean：`[0.50072, 0.01051, 0.13552]` m；
- 标准差：X 7.45 mm、Y 13.11 mm、Z 8.11 mm；
- 完整范围：X 28.38 mm、Y 64.04 mm、Z 32.70 mm；
- 中间 80% 范围：X 20.03 mm、Y 32.37 mm、Z 20.20 mm。

主相机初始画面中的蓝色方块位置：

- 所有 50 条合计约跨 24×27 px；
- 前 6 条和后续数据之间存在一次明显的相机/场景变化；
- Episode 8–49 共 42 条中，方块中心只跨约 8.4×4.8 px。

这说明 50 条 episode 并不等于 50 个充分不同的方块位置。大多数示范是在非常相似的场景布局中重复完成任务。

真实部署的两个 close target：

- safety-filtered success：约 `[0.47859, -0.01262, 0.13000]`；
- 最新 strict failure：约 `[0.46523, 0.03697, 0.13000]`；
- 二者相差约 51 mm。

因此模型并非完全输出固定轨迹，但当前证据表明：

- 它可能学到了一定的图像/状态相关位置修正；
- 它主要依赖狭窄的训练位置先验；
- 对训练范围外方块位置的视觉定位不可靠；
- 相同标准初始机器人状态使模型可以通过“机器人 state + 执行阶段”预测平均轨迹，而不必充分使用图像；
- 当前 50 条数据不足以证明任意位置的 object grounding。

## 11. 7 月 27 日当前结论

- 真实 Franka 部署链路已经能够稳定接收双相机、真实 state 和 7D action chunk；
- action timing 和 held-target publishing 已显著改善运动平滑性；
- 真实夹爪 feedback 已接入；
- 固定 XYZ close gate 已删除；
- 自动抬升未保留；
- 严格模式会暴露提前打开，而不是替模型修正；
- 当前 checkpoint 已证明能够接近、抓住、抬升、搬运和释放，但纯 VLA 成功仍未得到可靠重复验证；
- 最大模型问题是视觉位置泛化不足和 gripper close/open 时序不稳定。

---

# 四、三日综合成果

## 1. 数据成果

- 建立稳定的双相机 LeRobot 数据结构；
- 采集超过 100 个候选单 episode 目录；
- 完成 50 条筛选后示范的正式合并；
- 共有 10,743 帧与 100 个视频；
- 排除 `0036` 等夹爪状态无效数据；
- 证明 `observation.state.gripper` 必须来自真实反馈。

## 2. 模型成果

- 建立双相机 Cartesian delta-eef modality；
- 放弃继续依赖缺少腕部相机分布的旧真实模型；
- 从 LIBERO 30k checkpoint 直接适配双相机真实 Franka 数据；
- 完成 10k-step 训练并部署最终 checkpoint；
- 证明模型能产生真实接近、抓取、抬升、搬运和释放动作，但稳定性不足。

## 3. 工程成果

- 双 RealSense Docker 配置；
- Quest 3 wireless/USB bridge 与 ROS adapter；
- recorder 外部夹爪控制与反馈解耦；
- 真实 state normalization；
- 双相机图像预处理；
- dry-run/execute 分离；
- Cartesian workspace 与 action 限制；
- 真实 Franka 指宽反馈；
- 抓取宽度、稳定性和抬升验证；
- 平滑返回标准位姿脚本；
- action inference/publish timing 修正；
- 严格 VLA episode failure/complete 日志。

## 4. 研究结论

- 仿真 checkpoint 的高成功率不能代替真实系统验证；
- 少量同布局示范容易让 behavior cloning 学到位置/轨迹 shortcut；
- 客户端安全过滤可以提升完成率，但必须与纯 VLA 成功分开报告；
- 指宽约 28–30 mm 可以证明夹住当前方块，但不能证明方块已放到箱子上；
- 成功判定需要至少同时考虑：真实指宽、真实抬升、模型 release 和最终视觉任务结果；
- 真实硬件延迟会放大离散 gripper action 抖动，训练标签必须在抓取、抬升和搬运阶段持续保持 close。

---

# 五、下一阶段建议

## 1. 先进行安全恢复

- 当前/最近严格 episode 后夹爪可能以约 28.26 mm 宽度夹住方块；
- 在确认物体状态前不要直接重新部署；
- 安全释放或取下方块；
- 将 Franka 返回标准初始位姿；
- 确认 `/target_pose` 没有旧 publisher；
- 确认两路相机和 gripper feedback 都是新鲜数据。

## 2. 建立严格评估协议

每次运行单独保存日志，避免复用文件名覆盖历史记录。每个 episode 标记为：

- `Pure VLA success`：无模型动作覆盖，并且视觉确认方块在箱子上；
- `Safety-filtered VLA success`：任务完成，但 client 修改过模型动作；
- `VLA failure`：空抓、提前打开、未抬升、错误位置释放或超出安全范围。

日志至少记录：

- 初始机器人 pose；
- 初始方块位置/相机像素；
- 第一次 close 的 measured pose 和 target pose；
- 指宽时间序列；
- 抬升高度；
- release pose；
- 所有 safety intervention 次数；
- 最终视觉成功标签。

## 3. 扩充空间多样性数据

不要继续在同一位置盲目增加 episode 数量。建议：

- 在桌面标出 5×5 方块位置网格；
- 覆盖 left/center/right 与 front/center/back；
- 每个位置采集 3–4 条高质量示范；
- 新增约 75–100 条示范；
- 初期保持箱子和相机固定，只随机化方块；
- 后续再单独随机化箱子位置；
- 保留若干完整网格位置作为未见验证集；
- 每条示范的 close 必须持续到抬升和搬运结束。

## 4. 进行视觉敏感性测试

机器人保持同一个标准初始 state，仅改变方块位置：

- 左、中、右；
- 前、中、后；
- 每个位置至少重复两次。

比较：

- 初始 action chunk；
- 首次 close target；
- close target 是否随方块同方向、近似等量移动；
- 移除或遮挡方块后 action 是否明显变化。

如果模型输出几乎不随方块变化，则可以确认视觉 grounding 不足，而不是继续通过安全 gate 修补模型。

## 5. 训练策略

- 当前严格失败可用于 DAgger，但必须由人类提供正确修正轨迹；
- 优先补充方块位置多样性，而不是只增加同分布重复数据；
- 将 premature-open 失败片段对应的示范阶段重新采集，确保 close 标签持续；
- 新模型应使用固定 holdout 位置进行真实验证；
- 报告中分别给出纯 VLA 成功率和安全过滤成功率。

---

# 六、关键路径索引

## 数据

- 合并数据集：
  `/home/dase-hw101/franka_ws/dataset/snkdjn/quest3_franka_dualcam_pickplace_50eps`
- 合并清单：
  `dataset/snkdjn/quest3_franka_dualcam_pickplace_50eps/meta/merge_manifest.json`

## 训练

- Scheme A 启动脚本：
  `scripts/start_starvla_dualcam_50eps_from_libero30k_train.sh`
- 部署到服务器并启动训练：
  `scripts/deploy_and_start_starvla_dualcam_50eps_from_libero30k.sh`
- 双相机 modality：
  `scripts/starvla_quest3_franka_dualcam_delta_eef_modality.json`
- 最终 checkpoint：
  `/data/hanyu/starVLA_runs/quest3_franka_dualcam_50eps_from_libero30k_10k/final_model/pytorch_model.pt`

## 真实部署

- StarVLA Franka client：
  `scripts/starvla_franka_delta_pose_client.py`
- 返回标准位姿：
  `scripts/franka_return_to_standard.py`
- 通用 pose move：
  `scripts/franka_move_to_pose.py`
- 7 月 24 日部署记录：
  `deployment_logs/2026-07-24_starvla_franka_run.md`
- 7 月 27 日最新 trial log（注意该文件曾被复用/覆盖）：
  `franka:/home/ros/ros2_ws/deployment_logs/vla_trial_20260727_084222.log`

## 相机与录制

- 双相机 Docker Compose：
  `src/camera_driver/docker-compose.dual-franka.yaml`
- 双相机环境配置：
  `src/crisp_gym/crisp_gym/config/envs/dual_cam_franka.yaml`
- Recorder：
  `src/crisp_gym/crisp_gym/scripts/record_lerobot_format_leader_follower.py`
- Quest adapter：
  `src/crisp_gym/crisp_gym/scripts/quest3_stream_adapter.py`
