# 2026-08-04 StarVLA Spatial Forcing / 3D Prior 工作总结

日期：2026-08-04  
时区：Asia/Hong_Kong  
本地工作区：`/home/dase-hw101/franka_ws`  
服务器代码：`/home/hanyu/starVLA`  
任务：`pick up the cube and place it on the box`

---

## 0. 今日最终结论

今天完成了从 Spatial Forcing-style 3D teacher alignment、LoRA student、训练损失接入、短训练、matched A/B，到 held-out representation audit 的完整研究闭环。

最重要的结论是：

1. 已经成功把冻结的 VGGT 作为训练期 3D teacher 接入 StarVLA。
2. 部署路径仍然只使用 RGB QwenGR00T student，不需要 VGGT、深度相机或在线 3D 模型。
3. projected feature alignment 确实可以降低 alignment loss，但早期审计表明 alignment head 可能吸收大部分监督，student 本身没有明显获得更好的空间几何。
4. 为避免 projector shortcut，今天进一步加入了 projector-free relational geometry loss。
5. relational loss 的 20-step smoke、梯度更新和 500-step matched A/B 均成功运行。
6. 最终 held-out representation audit 得到：

   ```text
   Treatment − Control linear CKA:          +0.0528496
   Treatment − Control position RSA:        -0.1944963
   Treatment − Control shared-probe loss:   +0.0000224074
   ```

7. CKA 明显上升，证明 relational loss 确实改变了 Qwen student representation；这不是 alignment head 单独走捷径。
8. 但 position RSA 大幅下降，shared-probe loss 也朝错误方向变化，说明这种表示变化没有转化为更可用的方块位置几何。
9. 因此当前 Phase-8 Treatment 不能宣称“3D prior 提高了真实机器人空间理解”。
10. 当前不应继续 2k/20k，也不应进入真实 Franka 测试。
11. 下一条推荐路线是 scene-level memory-bank relational loss：在 batch size 为 1 时，跨训练步骤对齐不同场景/方块位置之间的 student 与 VGGT 距离关系。

今天最后的研究判断是：

```text
程序实现：PASS
训练稳定性：PASS
student representation 确实被改变：PASS
位置几何改善：FAIL
进入长训练/真机的条件：NOT MET
```

---

## 1. 项目背景与今天的目标

### 1.1 当前真实机器人 baseline

进入今天工作前，当前最可靠的真实 Franka baseline 是：

```text
74 条双相机真机示范
Libero-30k checkpoint 初始化
vision frozen
20k fine-tuning
safety-filtered deployment
20 次真机测试：13/20 = 65%
```

这里的 65% 属于 safety-filtered VLA，而不是 strict raw VLA。部署客户端包含：

- gripper chunk consensus；
- gripper switch confirmations；
- close latch；
- 真实夹爪宽度反馈；
- 抓取后真实抬升验证；
- workspace safety checks。

客户端没有使用固定方块坐标，也没有替模型自动完成抓取、抬升或释放。

### 1.2 今天希望解决的问题

此前真实部署最明显的问题是：

- 模型容易向训练数据中的常见方块位置运动；
- 方块稍微移动时，末端定位不够敏感；
- 有时在方块上方或后方下降；
- 抓取高度和夹爪关闭时机仍不稳定。

导师建议调研并加入可扩展的 3D/world prior。今天选择的主要方向是 Spatial Forcing-style training-only 3D teacher alignment：

```text
训练：RGB → Qwen student；同一 RGB → frozen VGGT teacher
监督：让 Qwen 中间视觉 token 学到 VGGT 的空间表示
部署：删除 teacher，只保留 RGB QwenGR00T policy
```

这个方向满足“有的数据源没有真实深度”的可扩展性要求：训练不依赖 RealSense metric depth 文件，部署也不需要深度输入。

---

## 2. 与此前 DAv2 gated cross-attention 路线的区别

此前实现过 `QwenGR00TSpatial + frozen Depth Anything V2-Small + gated cross-attention`：

```text
RGB
 ├─ Qwen visual backbone
 └─ frozen DAv2 geometry branch
        ↓
   projector + gated cross-attention
        ↓
   action head
```

该路线在部署时仍需要运行 DAv2 分支，因此属于 online geometry feature fusion。其 20k 模型离线指标尚可，但真实机器人多次出现越过方块、撞击方块后方桌面等问题。

今天的 Spatial Forcing-style 路线不同：

```text
训练期：
RGB → Qwen student tokens
RGB → frozen VGGT teacher tokens
student tokens ← alignment supervision → teacher tokens

部署期：
RGB → Qwen student → action head
VGGT 完全删除
```

因此今天的路线属于 training-time teacher prior / RGB-only distillation，而不是部署期在线深度或 3D feature fusion。

---

## 3. Spatial Forcing-style 技术管线

### 3.1 输入

每个样本包含两路 RGB：

1. primary third-person camera；
2. wrist camera。

数据仍来自：

```text
/data/hanyu/quest3_franka_real/snkdjn/
quest3_franka_dualcam_pickplace_74eps
```

训练配置保持：

```text
include_state: false
action_dim: 7
action_horizon: 8
per_device_batch_size: 1
Libero-30k 74eps model as pretrained checkpoint
```

### 3.2 Student

Student 是 QwenGR00T：

- 从 Qwen 中间层 `hidden_states[24]` 提取视觉 token；
- 根据 `<|image_pad|>` 连续 token span 分离 primary/wrist；
- 使用 Qwen 的 `image_grid_thw` 恢复二维 token grid；
- action head 仍是现有 QwenGR00T DiT action model。

### 3.3 Frozen VGGT teacher

训练时，同一组双相机 RGB 输入 frozen VGGT：

- VGGT 仅以 feature-only 模式运行；
- teacher 参数全部冻结；
- teacher 不接收梯度；
- teacher token 按二维 patch grid resize 到对应 Qwen token grid；
- primary/wrist 的顺序保持不变；
- 部署时不运行 VGGT。

VGGT 权重：

```text
/data/hanyu/starVLA_checkpoints/VGGT-1B/model.pt
```

### 3.4 LoRA student adaptation

Qwen backbone 主参数冻结，在以下 attention projection 中注入 LoRA：

```text
q_proj
k_proj
v_proj
o_proj
```

设置：

```text
LoRA rank: 32
LoRA alpha: 16
LoRA dropout: 0
adapter name: spatial_forcing
```

LoRA-B 初始为零，所以刚注入时 student 与原 baseline 完全一致；训练后通过 LoRA 更新改变 Qwen representation。

### 3.5 Projected alignment loss

第一版目标为：

```text
student token
   ↓ trainable MLP alignment head
projected student token
   ↓ cosine alignment
VGGT teacher token
```

损失形式：

```text
L_projected = mean(1 - cosine(project(student), teacher))
```

总损失最初为：

```text
L_total = L_action + alpha_projected × L_projected
```

这个方法的问题是 alignment head 可能独自学习 student→teacher 映射，而 Qwen student 本身变化很少。

---

## 4. 今日完成的 Spatial Forcing 实现阶段

### 4.1 Phase 2：核心 alignment 操作

实现并测试：

- Qwen 双相机 visual-token span 提取；
- Qwen 二维 token-grid 恢复；
- VGGT patch-grid 到 Qwen-grid 的二维插值；
- masked cosine alignment；
- camera view order 保持；
- 非法 token/grid/mask 检查。

中途遇到：

```text
ModuleNotFoundError: No module named 'starVLA.model.modules.spatial'
```

原因是 early package 中引用了未安装的旧 spatial module。之后把所需 token-span 逻辑放入 training-only `spatial_forcing/alignment.py`，解决了跨模块依赖。

### 4.2 Phase 3：VGGT teacher wrapper

完成：

- frozen VGGT feature-only wrapper；
- 双相机预处理；
- teacher device/dtype 管理；
- teacher `no_grad`；
- positional feature 支持；
- teacher 不进入正常 policy inference。

相关测试曾达到：

```text
Ran 6 tests
OK
```

### 4.3 Phase 4：LoRA 与训练总损失

完成：

- 安装 `peft==0.18.1`；
- Qwen attention LoRA 注入；
- plain baseline checkpoint 到 PEFT `base_layer` key 的兼容映射；
- `train_starvla.py` 使用 framework 返回的 `total_loss` 反向传播；
- projected alignment loss 加入训练目标。

服务端曾出现 `peft` 安装在错误 Python 环境、tests package import path 不一致等问题，最终使用 StarVLA conda Python 和文件路径形式运行 unittest 解决。

### 4.4 Phase 5：真实 VGGT smoke training

下载并验证约 5.03 GB 的 VGGT 权重后，完成真实数据 20-step smoke。

早期问题包括：

- GPU OOM；
- BF16 tensor 直接 `.numpy()` 报错；
- `/data` 空间不足；
- smoke checkpoint 约 9.4 GB。

BF16 eval 问题修复为在转 NumPy 前转成 float。最终 smoke 成功：

```text
SPATIAL_FORCING_SMOKE=PASS
```

### 4.5 Phase 6：ZeRO-safe 参数更新审计

DeepSpeed ZeRO-2 可能在 optimizer step 后清除或分片 `.grad`，直接看 gradient norm 会产生误判。

因此加入 before/after parameter snapshot：

```text
parameter_update_norm/spatial_forcing_lora_B
parameter_update_norm/alignment_head
```

它直接测量 optimizer step 前后参数变化，证明：

- LoRA student 的确更新；
- alignment head 的确更新；
- teacher 没有被训练。

---

## 5. 第一轮 projected-only 500-step 对照实验

### 5.1 Alpha=0.1 pilot

运行目录：

```text
/data/hanyu/starVLA_runs/
qwengroot_spatial_forcing_libero74_pilot500_alpha01_20260804_064832
```

W&B：

```text
https://wandb.ai/u3666250-the-university-of-hong-kong/
starVLA_Quest3_Franka/runs/whgekmgu
```

最终摘要：

```text
action_dit_loss:                         0.00876
alignment_loss:                          0.06260
parameter_norm/alignment_head:           1.40953
parameter_norm/spatial_forcing_lora_B:   0.13723
parameter_update_norm/alignment_head:    0.00222
```

### 5.2 Alpha=0 control

运行目录：

```text
/data/hanyu/starVLA_runs/
qwengroot_spatial_forcing_libero74_control500_alpha0_20260804_074900
```

W&B：

```text
https://wandb.ai/u3666250-the-university-of-hong-kong/
starVLA_Quest3_Franka/runs/yc5kv7m1
```

最终摘要：

```text
action_dit_loss:                         0.00674
alignment_loss:                          1.01491
parameter_norm/alignment_head:           1.38900
parameter_norm/spatial_forcing_lora_B:   0.12693
parameter_update_norm/alignment_head:    0
```

Alpha=0 时 alignment head 不更新，证明 alignment gradient 的开关有效。

---

## 6. Phase 7：第一次 representation audit

### 6.1 为什么不能只看 alignment loss

低 alignment loss 可能只说明 trainable alignment head 学会了映射，不能证明 Qwen student representation 具有更好的 3D geometry。

因此实现了 projection-resistant audit：

- linear CKA；
- cube-position RSA；
- direction-opposition score；
- shared held-out ridge probe；
- own-head cosine loss 仅作为辅助，不作为最终证据。

审计使用 5 个不发送机器人命令的双相机 snapshots：

```text
center
primary_front
primary_back
primary_left
primary_right
```

快照目录：

```text
/data/hanyu/starVLA_audit_inputs/dav2_cube_shift_20260803_144954
```

### 6.2 第一轮 projected-only 审计结果

输出目录：

```text
/data/hanyu/starVLA_runs/spatial_forcing_representation_audit_20260804_083101
```

对比结果：

```text
alpha0.1 − alpha0 linear CKA:          -0.00316667
alpha0.1 − alpha0 position RSA:        -0.00137334
alpha0.1 − alpha0 shared-probe loss:   -0.00000184
```

结论：

- CKA 没有改善；
- RSA 没有改善；
- probe loss 虽为负，但幅度几乎可以忽略；
- projected alignment head 很可能吸收了主要监督；
- 不值得直接把 Alpha=0.1 扩展到 2k/20k。

---

## 7. Phase 8：projector-free relational geometry loss

### 7.1 设计目的

为避免 alignment head shortcut，加入不经过任何 trainable projector 的关系损失。

对于 student 与 teacher token：

```text
S_student = normalize(F_student) × normalize(F_student)^T
S_teacher = normalize(F_teacher) × normalize(F_teacher)^T

L_relational = mean((S_student - S_teacher)^2)
```

特点：

- student 和 teacher feature dimension 可以不同；
- teacher 在函数内部再次 detach；
- 排除 Gram matrix 对角线，因为归一化后对角线恒为 1；
- 支持 mask；
- primary/wrist token 被拼接，因此损失同时包含：
  - primary 内部 token 关系；
  - wrist 内部 token 关系；
  - primary↔wrist 跨视角关系。

新总损失：

```text
L_total =
    L_action
    + alpha_projected × L_projected
    + alpha_relational × L_relational
```

同时保留旧 `alignment_alpha` 兼容字段，使 Phase 4–7 checkpoint/config 仍可读取。

### 7.2 测试覆盖

最终 alignment 相关 CPU 测试达到：

```text
Ran 12 tests
OK
```

覆盖：

- relational identity；
- orthogonal rotation invariance；
- token shuffle sensitivity；
- student 有梯度；
- teacher 无梯度；
- student/teacher 不同 feature dimension；
- mask；
- 对角线排除；
- training-only teacher-key checkpoint filter。

---

## 8. Phase-8 20-step smoke

使用：

```text
projected_alignment_alpha: 0.02
relational_alignment_alpha: 0.05
```

Step 20 结果：

```text
action_dit_loss:                         0.10698152
projected_alignment_loss:                0.44276631
weighted_projected_alignment_loss:       0.00885533
relational_alignment_loss:               0.03949964
weighted_relational_alignment_loss:      0.00197498
total_spatial_alignment_loss:             0.01083031
total_loss:                               0.11781183

parameter_update_norm/LoRA-B:             0.00156475
parameter_update_norm/alignment_head:     0.00436302
```

比例：

```text
weighted projected / action ≈ 8.3%
weighted relational / action ≈ 1.8%
total spatial / action ≈ 10.1%
```

结论：

- 所有 loss 有限；
- 总损失计算正确；
- LoRA-B 与 alignment head 都更新；
- 初始权重没有压制 action loss；
- 可以进入 matched 500-step pilot。

---

## 9. Phase-8 matched 500-step A/B

为了只测 relational loss 的增量效果，两组保持相同：

```text
相同 74eps 数据
相同 Libero74 baseline
相同 seed
相同 LoRA/action/alignment-head LR
相同 projected alpha = 0.02
相同 500 steps
```

唯一差异：

```text
Control:   relational alpha = 0
Treatment: relational alpha = 0.05
```

### 9.1 Matched Control 500

路径：

```text
/data/hanyu/starVLA_runs/
qwengroot_phase8_control500_proj002_rel0_20260804_090525/
final_model/pytorch_model.pt
```

W&B：

```text
https://wandb.ai/u3666250-the-university-of-hong-kong/
starVLA_Quest3_Franka/runs/kxkfbgll
```

结果：

```text
action_dit_loss:                         0.02396
projected alignment loss:                0.06245
mse_score:                               0.02593
parameter_norm/alignment_head:           1.40808
parameter_norm/LoRA-B:                   0.12515
parameter_update_norm/alignment_head:    0.00260
runtime:                                 4m28s
```

### 9.2 Matched Treatment 500

路径：

```text
/data/hanyu/starVLA_runs/
qwengroot_phase8_treatment500_proj002_rel005_20260804_090525/
final_model/pytorch_model.pt
```

W&B：

```text
https://wandb.ai/u3666250-the-university-of-hong-kong/
starVLA_Quest3_Franka/runs/nsb3ireh
```

结果：

```text
action_dit_loss:                         0.03940
projected alignment loss:                0.06488
mse_score:                               0.02695
parameter_norm/alignment_head:           1.40099
parameter_norm/LoRA-B:                   0.13185
parameter_update_norm/alignment_head:    0.00235
runtime:                                 5m36s
```

单个最终 batch 上 Treatment action loss 高于 Control；MSE 也略高约 3.9%。单个 batch 本身不能作为最终结论，因此继续进行了 held-out representation audit。

---

## 10. Checkpoint 中误存 VGGT teacher 的问题

### 10.1 现象

构建 RGB-only audit view 后，加载 checkpoint 曾多次报：

```text
Missing/Unexpected key(s):
spatial_teacher.model.aggregator....
```

### 10.2 根因

`FrozenVGGTTeacher.state_dict()` 已设计为空，但：

```text
Accelerator/DeepSpeed get_state_dict
```

可能直接从 distributed engine 收集参数，绕过 wrapper 的空 state-dict 逻辑。因此历史 checkpoint 存在两种格式：

1. 含 `spatial_teacher.*`；
2. 不含 `spatial_teacher.*`。

而 RGB-only config 不创建 teacher，strict load 因格式差异失败。

### 10.3 已完成修复

新增：

- `strip_spatial_teacher_state_dict()`；
- trainer 在 intermediate/final save 前过滤 teacher keys；
- Spatial-Forcing loader 兼容含/不含 teacher 的 checkpoint；
- audit loader 强制构建 RGB-only student；
- audit 单独加载一份共享 VGGT teacher；
- audit 对所有非-teacher missing/unexpected keys 保持严格检查；
- audit 明确打印：

  ```text
  RGB_ONLY_AUDIT_LOAD=PASS
  missing_non_teacher=0
  unexpected_non_teacher=0
  ```

最终 audit 成功，没有重新训练 Control/Treatment。

### 10.4 重要说明

这项修复影响未来保存的 checkpoint；今天已经生成的历史 checkpoint 仍可能包含 VGGT，因此文件较大。后续可以离线过滤并安全替换，但在确认 audit 与部署均可加载前，不应直接删除原文件。

---

## 11. Phase-8 最终 representation audit

程序结果：

```text
SPATIAL_FORCING_REPRESENTATION_AUDIT=PASS
ROBOT_COMMANDS_SENT=0
```

这里的 `PASS` 只代表：

- 所有模型成功加载；
- snapshots 成功处理；
- metrics 成功计算；
- 没有向 Franka 发送命令。

它不代表 Treatment 的方法效果通过。

最终 comparison：

```text
Treatment − Control linear CKA:
+0.05284956842660904

Treatment − Control position RSA:
-0.19449632614850998

Treatment − Control shared-probe cosine loss:
+0.00002240738831460476
```

### 11.1 CKA

`+0.05285` 是明显的正变化，表明：

- projector-free relational objective 确实影响了 student representation；
- Phase-7 中“alignment head shortcut”的问题部分得到解决；
- LoRA student 不再只是 action loss 下的普通更新。

### 11.2 Position RSA

`-0.19450` 是明显负变化，表明 Treatment 对五个方块位置之间的距离结构反而更不符合 VGGT teacher。

这是今天否决直接扩大训练的主要指标。

### 11.3 Shared probe

shared-probe loss 的期望方向是负值，即 Treatment 的 held-out teacher-feature prediction error 更低。

实际是：

```text
+2.24e-05
```

虽然绝对幅度不大，但方向错误，因此没有提供跨位置泛化改善证据。

### 11.4 最终方法判断

```text
CKA：           改善
position RSA：  明显恶化
shared probe：  略微恶化
action MSE：    略微恶化
```

所以不能因为 CKA 上升就进入真机测试。

---

## 12. 为什么 relational loss 改善 CKA，却恶化位置 RSA

当前训练配置：

```text
per_device_batch_size = 1
```

当前 relational loss 比较的是同一个 observation 内的 token-to-token Gram matrix：

```text
同一帧 primary 内部关系
同一帧 wrist 内部关系
同一帧 primary↔wrist 跨视角关系
```

它没有直接比较：

```text
cube at center
vs cube at front
vs cube at back
vs cube at left/right
```

因此它能够改善局部 token structure，使 CKA 上升；但没有约束不同场景/方块位置的 mean-pooled representation distance。

Phase-7/8 的 position RSA 恰好测量跨位置距离结构，所以两者出现分离是合理的：

```text
single-observation token geometry
≠
cross-observation object-position geometry
```

此外，74 条数据中的方块位置本身比较集中。如果训练数据缺少足够的位置变化，再强的 geometry loss 也难以学到大范围位置泛化。

---

## 13. GPU 与磁盘管理

### 13.1 GPU safety guard

20-step smoke 最初被安全拒绝：

```text
GPU 0 uses 9771 MiB
PID 1482087
ERROR: GPU 0 is occupied
```

这证明 runner 的 GPU 占用检查正常工作。GPU utilization 为 0% 不代表 GPU 空闲；policy server 仍可能持有大量显存。

### 13.2 磁盘

今天 `/data` 一度只剩约 13 GB。清理内容包括：

- 已有 `_rgb_inference` 时对应的重复 full checkpoint；
- 已完成 20k run 的中间 10k/20k checkpoint 副本；
- 50eps 完成训练的 2k/4k/6k/8k/10k 中间 checkpoint；
- 保留所有需要的 final model、config、W&B 与日志。

清理后：

```text
/data available: 78 GB
/ available:      26 GB
```

两个 Phase-8 500-step final model 随后成功保存。

后续原则：

- 500-step pilot 使用 `save_interval=1000`，只保存 final model；
- 不在根分区保存大型训练权重；
- 每次长训练前至少预留 30 GB；
- full 20k 前需要再次规划 checkpoint retention。

---

## 14. 今日主要代码与产物

### 14.1 核心模型与损失

```text
/home/dase-hw101/franka_ws/third_party/starVLA/
starVLA/model/framework/VLM4A/QwenGR00TSpatialForcing.py
```

包含：

- QwenGR00T Spatial-Forcing framework；
- VGGT teacher；
- LoRA student；
- projected alignment；
- relational alignment；
- total loss；
- ZeRO-safe update probes；
- checkpoint teacher filtering/compatibility。

```text
/home/dase-hw101/franka_ws/third_party/starVLA/
starVLA/model/modules/spatial_forcing/alignment.py
```

包含：

- visual-token extraction；
- grid reconstruction；
- teacher resampling；
- projected cosine loss；
- projector-free relational loss；
- teacher checkpoint key filtering。

### 14.2 Trainer

```text
/home/dase-hw101/franka_ws/third_party/starVLA/
starVLA/training/train_starvla.py
```

包含：

- `total_loss` 反向传播；
- projected/relational metric logging；
- ZeRO-safe parameter update metrics；
- save 前 teacher-key filtering。

### 14.3 Representation audit

```text
/home/dase-hw101/franka_ws/third_party/starVLA/
spatial_forcing_representation_audit.py
```

```text
/home/dase-hw101/franka_ws/third_party/starVLA/
starVLA/model/modules/spatial_forcing/representation_audit.py
```

包含：

- RGB-only checkpoint loader；
- frozen shared VGGT cache；
- linear CKA；
- position RSA；
- direction opposition；
- shared held-out ridge probe；
- JSON/CSV report。

### 14.4 Tests

```text
/home/dase-hw101/franka_ws/third_party/starVLA/
tests/test_spatial_forcing_alignment.py

/home/dase-hw101/franka_ws/third_party/starVLA/
tests/test_spatial_forcing_lora.py

/home/dase-hw101/franka_ws/third_party/starVLA/
tests/test_spatial_forcing_representation_audit.py
```

### 14.5 Server bundle

当前本地最新 bundle：

```text
/tmp/spatial_forcing_phase8_relational_20260804.tar.gz
SHA256:
285d3414c9f4b36143995555b4d3e2efab7ab1fcc529cf5da67b3323bd48e1d0
```

当前 audit script：

```text
SHA256:
a87fbc14a564ebc31e5ea90b584cd2bc8413645ed284f2da67d18f5b796982c3
```

---

## 15. 当前模型状态与推荐用途

### 15.1 真实机器人主 baseline

```text
/data/hanyu/starVLA_runs/
quest3_franka_dualcam_74eps_from_libero30k_vision_frozen_20k_retry3/
final_model/pytorch_model.pt
```

用途：

- 当前真实 Franka baseline；
- 已有 13/20 safety-filtered success；
- 后续新模块必须与它进行 matched comparison。

### 15.2 Phase-8 Control

```text
/data/hanyu/starVLA_runs/
qwengroot_phase8_control500_proj002_rel0_20260804_090525/
final_model/pytorch_model.pt
```

用途：下一版 scene-level relational 的 matched control。

### 15.3 Phase-8 Treatment

```text
/data/hanyu/starVLA_runs/
qwengroot_phase8_treatment500_proj002_rel005_20260804_090525/
final_model/pytorch_model.pt
```

用途：研究记录和消融对照。当前不推荐真机部署或扩展训练。

---

## 16. 下一步推荐：scene-level memory-bank relational loss

### 16.1 动机

需要直接约束跨 observation 的位置结构，但 GPU 内存不适合简单把 batch size 从 1 大幅提高。

推荐维护 detached feature queue：

```text
student_queue: 历史 K 个 student scene features
teacher_queue: 历史 K 个 VGGT scene features
K: 32–64
```

当前样本计算：

```text
s_current = pool(student tokens)
t_current = pool(teacher tokens)

student_similarity = cosine(s_current, student_queue)
teacher_similarity = cosine(t_current, teacher_queue)

L_scene = MSE(student_similarity, teacher_similarity)
```

队列中历史 student/teacher feature 全部 detach；梯度只流向当前 student。

### 16.2 推荐按视角计算

分别维护或计算：

```text
primary scene relation
wrist scene relation
optional dual-view fused scene relation
```

这样可以避免一个视角主导另一个视角。

### 16.3 下一轮实验顺序

1. 实现 queue、warmup、enqueue/dequeue 和日志。
2. 增加 CPU unit tests：
   - queue FIFO；
   - teacher/old student detach；
   - current student 有梯度；
   - batch size 1 可工作；
   - queue 未满时安全处理。
3. 20-step smoke，记录：
   - `scene_relational_loss`；
   - weighted scene loss；
   - queue fill；
   - LoRA-B update norm；
   - action loss。
4. 根据实际尺度选择 scene alpha，使 weighted geometry loss 初期约为 action loss 的 5–10%。
5. 做 matched 500-step：
   - Control：projected 0.02，scene relational 0；
   - Treatment：projected 0.02，scene relational tuned alpha。
6. 重跑同一组 held-out audit。
7. 进入 2k 的最低条件：
   - CKA 不下降；
   - position RSA 必须为正向改善；
   - shared-probe loss 必须下降；
   - action MSE 不应恶化超过约 5%。
8. 只有 2k 离线指标通过后，才运行 open-loop。
9. open-loop 通过后，才进行少量安全真机 A/B。

### 16.4 数据限制

现有 74 条数据中的方块位置变化仍比较集中。scene-level loss 只能利用数据中真实存在的变化。

若 memory-bank audit 仍失败，应优先：

- 收集更系统的前/后/左/右位置变化；
- 保持初始机器人位姿和相机标定一致；
- 平衡不同位置的 episode 数量；
- 避免所有示范都从近乎同一抓取轨迹开始。

---

## 17. 今天可以陈述与不应陈述的结论

### 可以陈述

1. 成功实现了 Spatial Forcing-style frozen VGGT teacher + Qwen LoRA student。
2. teacher 仅在训练/审计运行，部署为 RGB-only。
3. projected-only alignment 存在 projector shortcut 风险。
4. projector-free relational loss 显著改变了 student representation，CKA 提高约 0.053。
5. 当前 relational objective 没有改善跨方块位置的空间几何，RSA 下降约 0.1945。
6. 因此通过 representation audit 阻止了一个不成熟模型进入真机，评估流程发挥了作用。

### 不应陈述

1. 不应说 Phase-8 Treatment 已提高真实机器人成功率。
2. 不应说 CKA 上升等价于 3D understanding 提升。
3. 不应把 `SPATIAL_FORCING_REPRESENTATION_AUDIT=PASS` 表述为方法成功。
4. 不应在没有 RSA/probe 改善前扩大到 20k。
5. 不应在没有离线与 open-loop 通过前做真实 Franka 长测试。

---

## 18. 最终一句话总结

2026-08-04 完成了 StarVLA 的 Spatial Forcing-style 3D teacher 全流程复现与两轮消融：projector-free relational loss 成功进入 Qwen student 并提高 CKA，但由于 batch size 1 下只约束单场景 token geometry，没有学到跨方块位置的场景几何，最终 RSA 和 shared probe 变差；因此停止扩展训练，下一步转向 scene-level memory-bank relational distillation，并继续以 Libero74 20k 的 65% safety-filtered 真机结果作为主 baseline。
